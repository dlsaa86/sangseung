# KNOWN ISSUES

1차 프로토타입 자율 구현 중 남은 문제와 확인이 필요한 항목.
작성 시작: 2026-07-28

---

## A. 환경 / 도구

### A-1. Gemini CLI 사용 불가 (해결 불가 — 계정 문제)
```
IneligibleTierError: This client is no longer supported for
Gemini Code Assist for individuals.
```
`gemini-cli` 무료 티어가 중단되어 조사·독립검증·시각분석 역할을 쓸 수 없다.
계속 쓰려면 Antigravity로 마이그레이션이 필요하다.

**대응:** 독립 검토를 Claude가 직접 수행했다. 실제로 T-03에서 배수 폭주 결함,
T-06에서 과적 불가능 결함을 이 방식으로 잡았다. 다만 **제3자 시선이 없다**는
구조적 약점은 남아 있다 — 구현자와 검토자가 같은 모델이다.

### A-2. Unity가 Play 모드로 방치되어 있었다
작업 시작 시 에디터가 Play 모드였고 활성 씬이 이름 없는 씬이었다.
이 상태에서는 스크립트가 재컴파일되지 않아 어떤 변경도 반영되지 않는다.
종료 후 `Prototype_Elevator.unity`를 열고 진행했다.

**재발 방지:** 자동화 작업 전 `EditorApplication.isPlaying`을 먼저 확인할 것.

### A-3. 에디터 포커스 손실 시 플레이 루프 정지
`runInBackground=false`라 에디터가 포커스를 잃으면 플레이 모드가 멈춘다.
Play Mode 자동 검증 결과가 진행되지 않으면 이것을 먼저 의심할 것.
(프로젝트 `CLAUDE.md`에 기록된 기존 제약)

---

## B. 밸런스 리스크 (시뮬레이션으로 확인 필요)

### B-1. 상층부 요구 전력 과다 — **기각됨 (시뮬레이션으로 확인)**

> **결론: 우려한 "클리어 불가"는 사실이 아니었다.**
> 12런 결과 성공 6/12(50%), 평균 최고층 8.2. 조정하지 않았다.
> 다만 **9층 통과율 25%** 로 마지막 층이 병목이며, 평균 달성 전력이
> 요구를 밑도는 유일한 층이다. 이건 "불가능"이 아니라 "벽"이므로
> 사람 플레이테스트에서 좌절감으로 느껴지는지 확인이 필요하다.

<details>
<summary>기각 전 원래 우려 (기록용)</summary>

현재 설정:
- 요구 전력: 층0 = 100, 층9 = 325 (`+25/층`)
- 한 턴 예상 산출: 구슬 출력합 15~105 + 조합 기본점수 5~30, 배수 1.0~2.5
  → 대략 30~150. 층당 3턴이면 **90~300**

승객 효과(기술자 +2/턴, 변압기 ×2)를 감안해도 층 8~9에서 요구 전력을 넘기기
어려울 수 있다. 그러면 10층 런이 사실상 클리어 불가가 된다.

**확인 방법:** T-08 시뮬레이션의 정책별 평균 최고 도달 층.
평균이 6층 미만이면 `requiredPowerGrowthPerFloor`를 낮추거나
`CombinationConfig`의 배수를 올려야 한다.

**아직 조정하지 않은 이유:** 추측으로 수치를 바꾸면 근거가 남지 않는다.
시뮬레이션 데이터를 받은 뒤 조정하는 편이 낫다.
</details>

### B-1b. 조합 실패율 59% — **신규 발견, 우선순위 높음**
315턴 중 186턴(59.0%)이 `None`(조합 실패)이었다. 성공 41% 중에서도
절반 이상이 `ContainsLegendary`(14.3%)와 `ThreeSameGrade`(14.0%) 두 종류에 몰렸다.

`ThreeOfAKind`(1.6%) · `ThreeDifferentCommon`(1.6%) · `SpecificOrder`(1.3%)는
합쳐서 4.5%로 **사실상 등장하지 않는다.** 9종 구슬에서 3개를 뽑는 구조에서
"같은 구슬 3개"는 확률적으로 거의 불가능하기 때문이다.

밸런스 수치 문제가 아니라 **조합표 설계 문제**다. 조합 규칙 자체가
현재 구슬 풀 크기에 맞지 않는다.

단, 59%가 "답답하다"인지 "기본 출력이 남으니 괜찮다"인지는 수치로 판단할 수 없다.
**→ HUMAN PLAYTEST REQUIRED**

### B-2. 과적 빌드의 기대값 — **부분 확인**
과적형 정책은 33개 층 시도 중 20개가 과적 상태였고 사고 3회(약 15%)가 났다.
비과적 빌드는 사고 확률 0%로 완전히 안전했다. **과적은 실제 위험으로 작동한다.**

다만 성공률은 세 정책 모두 2/4로 동일해 **과적이 이득인지 손해인지 판별되지 않았다.**
정책당 n=4는 통계적으로 무의미하다. 정책당 50런 이상 재측정이 필요하다.

### B-2b. 빌드 차별화가 결과에 나타나지 않음 — **미확인, 재측정 필요**
경량형 2/4, 균형형 2/4, 과적형 2/4로 **세 정책의 성공률이 완전히 같았다.**
빌드 방향이 결과를 바꾸지 못한다면 선택의 의미가 사라진다.

다만 **정책당 n=4는 통계적으로 무의미하다.** 이 수치로 결론을 내리면 안 된다.

한편 최종 돈은 균형형 1223 / 경량형 652 / 과적형 288로 4배 이상 벌어졌다.
초과 전력 분배 선택은 확실히 결과를 바꾸고 있다.

**확인 방법:** `PlaytestSimRunner.RunsPerPolicy`를 4 → 50 이상으로 올려 재측정.
그래도 성공률이 같으면 승객 효과의 크기 자체를 키워야 한다.

### B-3. 완벽 정지 허용 오차가 임의값
`perfectStopTolerance = 0.12` (월드 단위). 구슬 간격 대비 어느 정도인지
사람이 쳐 봐야 안다. 너무 좁으면 도박사가 무의미하고, 너무 넓으면
모든 정지가 완벽 정지가 되어 긴장이 사라진다.

**→ HUMAN PLAYTEST REQUIRED**

---

## C. 구현 범위상 남은 것

### C-1. Probability 효과가 아직 구슬 추첨에 연결되지 않았다
`ProbabilityEffectHandler`가 `GenerationContext.PendingProbabilityModifiers`에
효과를 적재하지만, `RouletteController`가 이를 읽어 추첨 가중치를 바꾸는
연결이 없다. 구조는 준비되어 있고 소비처만 없는 상태다.

7종 효과 중 유일하게 **실제 게임플레이 영향이 없는 효과**다.
"구슬 확률을 빌드로 바꾼다"는 99번 문서의 핵심 기둥 중 하나이므로
다음 단계에서 우선 연결해야 한다.

### C-2. 승객 하차·거래·목적지 없음
02번 문서의 승하차 흐름, 강제 하차 대가, NPC 목적지 보상은 1차 범위 밖이다.
현재는 태우면 런이 끝날 때까지 유지된다.

### C-3. 로그 접두사가 일부 불일치
효과 파이프라인 일부 로그가 `[상승]`이 아니라 `[EffectPipeline]`으로 나간다.
기능에는 영향이 없지만 로그 필터링이 불편하다.

---

## D. 사람이 직접 확인해야 하는 항목 (자동 검증 불가)

아래는 **AI가 검증했다고 표시할 수 없는** 항목이다. 전부 실제 플레이가 필요하다.

- **HUMAN PLAYTEST REQUIRED** — 세 통관을 동시에 읽고 각각 멈추는 것이 긴장감 있는가
- **HUMAN PLAYTEST REQUIRED** — 제동 지연이 납득 가능하고 학습 가능한가
- **HUMAN PLAYTEST REQUIRED** — 낙하 중에 구슬 9종과 등급이 실제로 구분되는가
- **HUMAN PLAYTEST REQUIRED** — 무거운 승객을 태우는 선택이 진짜 고민이 되는가
- **HUMAN PLAYTEST REQUIRED** — 과적 사고가 감수한 위험으로 느껴지는가, 억울한가
- **HUMAN PLAYTEST REQUIRED** — 초과 전력의 상승/돈 선택이 매번 의미 있는가
- **HUMAN PLAYTEST REQUIRED** — 조합 실패 시에도 다음으로 이어지는 최소 가치가 남는가
- **HUMAN PLAYTEST REQUIRED** — 다시 플레이하고 싶은가
