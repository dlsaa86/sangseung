# 유니티가 생성하는 `.csproj` 와 달리 이 파일은 **손으로 쓴 것**이다.
# 루트 `.gitignore` 의 `*.csproj` 에서 되살린다 — 이게 없으면 하네스가 아예 빌드되지
# 않는데, 그 사실은 다른 기기에서 클론한 뒤에야 드러난다.
!Headless.csproj

# 산출은 커밋하지 않는다. 표본 수가 다른 보고서가 같은 이름으로 섞이면 어느 쪽이
# 무엇인지 판정할 수 없게 된다 — 채택할 것만 사람이 `docs/runtime/` 으로 옮긴다.
out/
bin/
obj/
