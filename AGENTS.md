# Upandup_DDD

Unity **6000.5.5f1** / URP 17.5.0. 엘리베이터 상승 + 룰렛 튜브 프로토타입.

이 파일이 **에이전트 지침의 유일한 원본**이다. `CLAUDE.md`는 이 파일을 가리키는
심볼릭 링크다 — Claude Code와 prime-agent가 서로 다른 지침을 읽는 상황을 만들지 않는다.
(2026-08-05 독립 감사 F-7: 문서 둘이 다른 말을 하면 어느 쪽이 현행인지 판정할 수 없다.)

```
docs/                              AI 개발 동결 명세
  MASTER_PRD.md                    최상위 제품 요구사항
  TECH_SPEC.md                     Unity 구조·상태·테스트 기준
  CURRENT_PHASE.md                 현재 세션 허용 범위
  VISUAL_SPEC.md                   비주얼 방향과 평가 기준
  VISUAL_CRITERIA.md               상세 시각 평가 기준 (코드 주석이 항목 번호로 인용한다)
  VISUAL_VERIFY_LOOP.md            캡처 기반 시각 회귀 절차
  DECISION_LOG.md                  확정 결정과 변경 이력
  ASSUMPTION_LOG.md                임시 기본값과 교체 위치
  TOPDOWN_MASTER_BACKLOG.md        탑다운 단일 작업 목록
  runtime/TOPDOWN_PROGRESS.md      현재 패스·다음 항목·차단 사항
  runtime/PENDING_DECISIONS.md     사용자 결정이 필요한 항목과 기본 프리셋
  runtime/VISUAL_VERDICT.md        독립 시각 평가 판정 (구현자가 쓰지 않는다)
tools/                             유틸 스크립트 (캡처 측정, 씬 감사 등)
Assets/Prototype_Elevator/
  Scenes/Prototype_Elevator.unity   유일한 작업 씬
  Scripts/{Core,Data,Effects,Roulette,UI}
  Data/PrototypeConfig.asset        밸런스 값
Assets/CaptureHarness/              결정론적 캡처 하네스 (README 참조)
Assets/Plans/                       T-00~ 작업 명세
production/                         Story·스프린트·세션 로그
design/                             GDD·엔티티 레지스트리
Captures/                           캡처 산출물 (gitignore, 기기 종속)
```

네임스페이스: 게임 코드 `Ascend.Prototype`, 하네스 `Ascend.CaptureHarness`.
**asmdef이 없다** — 모든 스크립트가 `Assembly-CSharp` 하나에 들어간다. 스크립트를
하나만 고쳐도 전체가 재컴파일되고 플레이 모드가 종료된다.

---

# 도구 분담 — 하이브리드

두 에이전트를 쓴다. **역할이 갈리는 이유는 취향이 아니라 MCP 전송 방식이다.**

| | Claude Code | prime-agent |
|---|---|---|
| 담당 | Unity 씬·프리팹·머티리얼, 플레이모드, 고정 캡처, 시각 평가 | 순수 C# 모듈, 테스트, 문서, 리팩터링, 장기 자율 작업 |
| MCP | unity-mcp · serena · blender (전부 **stdio**) | **HTTP 서버만 지원** — stdio 항목은 호스트가 조용히 버린다 |
| 자율 루프 | Ouroboros (`~/.claude/plugins`, 전역 설치) | 내장 `/goal` · autonomous budget · heartbeat |
| 설정 | `.claude/settings.json` (permissions 만) | `.prime/agent/settings.json` |

**Unity를 만지는 일은 전부 Claude Code 쪽이다.** prime-agent는 Unity 에디터를 볼 수도
움직일 수도 없으므로, 씬·프리팹·머티리얼·캡처가 얽힌 작업을 prime-agent에게 주지 않는다.

**둘을 동시에 같은 워킹 트리에서 자율 루프로 돌리지 않는다.** 번갈아 쓴다.
동시 실행은 아래 「직렬화 에셋」 절이 막으려는 손상을 그대로 재현한다.

---

# 절대 규칙 — 어느 도구에서도 뒤집히지 않는다

`.unity`, `.prefab`, `.mat`, `.asset`은 전부 YAML이고 내부가 fileID로 상호 참조된다.
두 곳에서 같은 파일을 고치면 머지가 깨지는 게 아니라 **조용히 손상된다** — 텍스트로는
병합되지만 참조가 어긋나 런타임에 터진다. 되돌리기도 어렵다.

1. **직렬화 에셋을 `Write`/`Edit`로 텍스트 편집하지 않는다.** 되돌릴 일이 있으면
   `Unity_RunCommand`로 에디터 안에서 고치고 `SaveScene`한다.
2. **동시에 두 주체가 씬을 열지 않는다.** 에디터 인스턴스도 하나만 띄운다.
3. **에디터가 살아 있는 동안 워킹 트리를 다시 쓰는 git 명령을 쓰지 않는다**
   (`checkout`/`restore`/`switch`/`stash`/`reset --hard`/`clean`/`revert`/`merge`/
   `rebase`/`pull`/`cherry-pick`/`apply`/`am`). 꼭 해야 하면 `-- <경로>`로 범위를
   좁히고 그 경로가 `.unity`/`.prefab`/`.mat`에 닿지 않는지 확인한다.
4. **구현자가 자기 작업을 `VERIFIED`로 최종 승인하지 않는다.** 시각 항목은
   `docs/runtime/VISUAL_VERDICT.md`의 ACCEPT, 로직 항목은 독립 감사가 승격시킨다.

**이 넷을 지켜 주던 훅은 없다.** 2026-08-03에 걷어냈고 2026-08-04에 일부 복원했다가
2026-08-08에 다시 전부 지웠으며, 2026-08-09 prime-agent 전환에서 스크립트까지 삭제했다.
**전부 이 저장소가 실제 실패에서 얻은 규칙이다. 장치가 없어졌다고 규칙이 없어진 게 아니다.**

---

# 문서 권한과 시작 순서

작업을 시작하기 전에 반드시 다음 순서로 읽는다. 충돌 시 앞선 문서가 우선한다.

1. `docs/MASTER_PRD.md` — 제품 범위
2. `docs/TECH_SPEC.md`
3. `docs/CURRENT_PHASE.md` — **이번 세션에서 구현할 수 있는 범위를 제한한다**
4. `docs/VISUAL_SPEC.md`
5. `docs/DECISION_LOG.md`
6. `docs/ASSUMPTION_LOG.md`
7. 관련 `Assets/Plans/` 티켓

Notion은 편집 가능한 기획 원본이지만 실제 구현 중에는 `docs/`의 동결 스냅샷을 기준으로 한다.
Notion이 다르다고 판단되면 임의로 기준을 바꾸지 말고 `DECISION_LOG.md`에 변경 제안을 적는다.

작업 시작 시: ① 현재 코드·씬·에셋을 요구사항에 매핑 ② 누락·충돌·부채를
`docs/runtime/GapAnalysis.md`에 ③ 작업 순서와 검증 계획을
`docs/runtime/ImplementationPlan.md`에 ④ 막지 않는 판단은 `docs/ASSUMPTION_LOG.md`에.

---

# 탑다운 자율 실행 규칙

장시간 자율 개발은 `docs/TOPDOWN_MASTER_BACKLOG.md`의 **네 패스**를 순서대로 통과한다.
이 규칙들은 취향이 아니라 이 프로젝트가 실제로 겪은 실패에서 나왔다.

1. **Pass 1·2가 끝나기 전에는 한 대상을 두 번 이상 연속 미세 개선하지 않는다.**
   전체 Coverage 이전의 폴리싱은 아직 존재하지도 않는 것과 비교되지 않은 개선이다.
2. **비주얼 평가 실패는 기록하고 다음 미구현 필수 범위로 이동한다.** REJECT는 종료
   사유가 아니다. **같은 층위에서 세 번 실패한 항목은 네 번째를 시도하지 않는다** —
   필요한 것은 노력이 아니라 구조 변경이나 배치 결정이다.
3. **전체 Coverage 이전에는 정밀 폴리싱보다 누락 시스템 구현을 우선한다.** Pass 1의
   차단 조건은 셋뿐이다 — 컴파일 오류, 데이터 손상, 진행 불가.
4. **컴파일을 깨뜨린 상태로 다음 기능으로 이동하지 않는다.** asmdef이 없어 스크립트
   하나가 전체를 막는다.
5. **되돌릴 수 있는 결정은 질문하지 않고 기본값으로 진행한다.** 되돌리기 어려운 것만
   `docs/runtime/PENDING_DECISIONS.md`로 올리고, 그 항목 때문에도 멈추지 않는다.
6. **구현자는 자신의 작업을 VERIFIED로 최종 승인하지 않는다.**
7. **20~30분 단위의 복구 가능한 마일스톤마다 저장 → 테스트 → 진행 로그 → 로컬 커밋.**
   커밋 전에 `docs/runtime/TOPDOWN_PROGRESS.md`를 갱신한다. 코드 변경 뒤에는
   `Ascend/Run Self Tests`를 돌리고 커밋한다 — **안 돌리면 아무도 안 잡는다.**
8. **원격 push, PR 생성, main 병합은 사용자가 명시적으로 요청할 때만.**

## 완료 판정 — 규율이다

| 패스 | 완료 기준 |
|---|---|
| **Pass 1** | **그 패스가 소유한** Required가 최소 `SKELETON`·`VISIBLE` |
| **Pass 2** | 소유 Required가 최소 `CONNECTED` (+ 끊긴 증거 경로) |
| **Pass 3** | 백로그 §1 `PASS3_GATED` 28항목 `VERIFIED` + **독립 평가가 현재 캡처에 대해 돌았을 것** |
| **Pass 4** | **모든** Required `VERIFIED` + 시각 `ACCEPT` + 전체 테스트·빌드·10층 증거·미커밋 0 |

**소유 패스**는 각 백로그 항목의 `· 패스:`에서 가장 이른 패스다.
Pass 4는 소유권과 무관하게 전부 요구하므로 **미룬 것이 사라지지 않는다.**

> Pass 3은 `ACCEPT`를 요구하지 **않는다** (2026-08-02 사용자 결정, `DECISION_LOG.md:384-402`).
> `ACCEPT`의 정의는 「판독성·스타일 평균 4.0 이상 · 2점 이하 없음」이고 그건 VERIFIED급
> 기준이다. Pass 3의 바는 CONNECTED라 두 층위가 어긋나 있었다. 그래서 Pass 3은
> **평가가 현재 그림에 대해 돌았는가**만 묻고 `ACCEPT`는 Pass 4로 옮겼다 —
> 기준을 낮춘 것이 아니라 묻는 시점을 바로잡은 것이다.

**아무것도 자동으로 강제되지 않는다.** 모델이 직접 대조해야 하고, 대조하지 않으면 그냥
통과한다. 그래서 **미룬 요구는 반드시 적는다** — 안 적으면 게이트 완화가 곧 「사라진
요구사항」이 된다.

## Pass 1 작업 리듬 (고속 Coverage)

- **항목마다** 전체 테스트·빌드·캡처·독립 평가를 수행하지 **않는다.**
- 연관된 **5~10개 항목을 한 배치**로 구현한 뒤 컴파일 + 최소 스모크 테스트를 한 번.
- 플레이스홀더·단순 형상·기본 데이터·임시 UI를 적극 쓴다.
- 진행 문서는 **배치 종료 시에만** 갱신한다. 커밋은 45~90분 단위 기능 묶음.
- 씬 오브젝트를 반복 수작업 배치하지 말고 **데이터 기반 런타임 생성기 또는 재실행
  가능한 Editor 조립 스크립트**를 쓴다 — 직렬화가 유일한 병목이기 때문이다.

---

# 막혔을 때 — 질문하고 멈추지 않는다

사용자가 없는 동안 질문을 띄우고 대기하면 그 시간 전체가 낭비된다.

**스스로 처리한다:** 컴파일 오류 · NullReferenceException · 잘못된 컴포넌트 연결 ·
테스트 실패 · 좌표/크기/레이아웃 조정 · 성능 저하 · 명명 규칙 · 기존 설계에 답이 있는 문제.
되돌릴 수 있는 결정은 기본값으로 진행하고 `docs/ASSUMPTION_LOG.md`에 적는다.

**해당 작업만 보류하고 다른 독립 작업을 계속한다:** 두 설계안이 모두 타당한 창작 결정 ·
GDD와 사용자 지시의 충돌 · 핵심 구조 재작성 · 저장 데이터 손상 가능성 · 대규모 에셋 삭제 ·
외부 유료 서비스. 보류 항목은 `docs/runtime/PENDING_DECISIONS.md`에 **교체 가능한 기본
프리셋과 함께** 적는다. 세션 전체를 멈추지 않는다.

## 자율 루프 종료 조건

아래 중 **하나라도** 걸리면 즉시 멈추고 보고한다. **세는 장치가 없으므로 직접 센다.**

```
- 모든 acceptance criteria 통과          ← 정상 종료
- 같은 실패가 3회 반복
- 같은 파일을 5회 이상 재작성
- 품질 점수가 3회 연속 개선되지 않음
- 범위 밖 변경이 필요함
- Unity가 정상적으로 열리지 않음 / 모달에 잠김
- 컨텍스트 또는 사용량 한계 접근
```

## 자율 작업 중 금지

```
main 직접 수정·병합 · 원격 push · PR 생성 · 핵심 에셋 삭제 · 대규모 폴더 이동
Unity 버전 변경(6000.5.5f1 고정) · 렌더 파이프라인 변경(URP 17.5.0 고정)
패키지 대량 설치·업그레이드 · GDD 핵심 규칙 변경 · 아트 방향 변경
레퍼런스에 없는 신규 시스템 발명 · 유료 API·에셋 구매 · 환경변수·비밀키 접근
```

앞의 넷은 `.claude/settings.json`의 `deny`/`ask`로도 막혀 있다. 나머지는 규율이다.

---

# 구현과 실패 대응

- 이미 정상 동작하는 시스템을 이유 없이 재작성하지 않는다.
- 핵심 규칙과 연출을 분리하고, 가변 값은 데이터 또는 프로파일로 둔다.
- 패키지, Unity 버전, URP 버전을 사용자 승인 없이 변경하지 않는다.
- 빌드 또는 핵심 테스트가 깨진 상태에서 새 기능을 추가하지 않는다.
- 동일 오류를 세 번 이상 같은 방식으로 수정하지 않는다.
- 반복 실패 시 최소 재현 테스트를 만들거나 비차단 기능을 인터페이스 뒤 플레이스홀더로 격리한다.
- 실패한 테스트를 삭제, 무시, 조건부 스킵으로 숨기지 않는다.
- 유료 에셋, 외부 유료 API, 라이선스가 불명확한 파일을 추가하지 않는다.
- 승인 대기 비주얼·모션·밸런스는 하나의 최종안으로 잠그지 않는다.

---

# 검증

각 마일스톤은 다음 루프를 수행한다.

`구현 → 컴파일·자동 테스트 → 실제 플레이 → 고정 캡처 → 시각 평가 → 성능 측정 → 수정 → 재검증`

화면에 보이는 것을 바꿨으면 `docs/VISUAL_VERIFY_LOOP.md`의 절차를 따른다.
로직 전용 변경에는 쓰지 않는다 (캡처 1회에 수십 초 걸린다).

- **구현자가 자기 결과를 최종 평가하지 않는다.** 평가자는 어느 쪽이 새 빌드인지 모른 채 본다.
- **직전 승인 빌드보다 나빠지면 채택하지 않는다.**
- 테스트·캡처·성능 기준을 통과하지 않은 기능을 완료로 선언하지 않는다.
- 실패와 미완료를 숨기지 않고 재현 절차와 원인을 기록한다.

베이스라인(`Captures/baseline.txt`)은 **기기에 종속된다.** OS·GPU·그래픽 API가 다르면
렌더 결과가 비트 단위로 다르므로, 기기를 옮기면 그 기기에서 새로 세운다.
manifest의 `machineFingerprint`가 이를 강제한다.

---

# 환경 주의사항

시간을 크게 낭비하게 만드는 것들이다.

**에디터 로그는 프로젝트 상대 경로에 있다** — `Logs/Editor.log`.
`%LOCALAPPDATA%\Unity\Editor\Editor.log`가 아니다. MCP의 콘솔 조회는 Clear-on-Play
이후 비어 보이므로, 플레이 모드 결과는 이 파일에서 확인한다.

**MCP `Unity_RunCommand`는 `System.Reflection`을 차단한다.** 타입을 직접 참조하거나
`AppDomain.CurrentDomain.GetAssemblies()`로 우회한다.

**`isCompilationSuccessful: true`는 프로젝트가 컴파일된다는 뜻이 아니다.** 그 스니펫이
**현재 로드된 옛 어셈블리에 대해** 컴파일됐다는 뜻일 뿐이다. 프로젝트에 컴파일 오류가
있으면 도메인이 리로드되지 않아 **에디터는 옛 코드를 계속 실행하고**, `.cs`를 고쳐도
결과가 **비트 단위로 동일하게** 나온다. 이때 `AssetDatabase.Refresh(ForceUpdate)`도
`EditorUtility.RequestScriptReload()`도 `CompilationPipeline.RequestScriptCompilation()`도
전부 조용히 아무 일도 하지 않는다.

그래서 **`.cs`를 고친 뒤 첫 검증은 항상 이것이다.**

```bash
grep -an "error CS" Logs/Editor.log | tail -5    # 줄 번호로 최신 여부를 판단한다
```

로그에는 옛 세션의 오류가 남아 있으므로 **줄 번호가 파일 끝에 가까운 것만** 현재
오류다. 오류를 고친 뒤 리프레시하면 그 호출이 120초 타임아웃까지 매달리는데,
그것이 도메인 리로드가 실제로 걸렸다는 **신호**다 — 실패가 아니다.

값이 반영됐는지는 인상이 아니라 상수로 확인한다.

```csharp
result.Log((ReferenceRoomSpec.BankRibWidth * 1000f).ToString("F0") + "mm");
```

**에디터가 포커스를 잃으면 플레이 루프가 멈춘다** (`runInBackground=false`).
자동화 중 플레이 모드가 진행되지 않으면 이걸 먼저 의심한다.

**`OpenScene`은 참조되지 않은 ScriptableObject의 네이티브 객체를 파괴한다.**
씬을 열기 전에 필요한 값을 미리 읽어둔다.

**플레이 모드 진입은 도메인 리로드 때문에 수십 초 걸린다.** 폴링하지 말고 백그라운드로
돌린 뒤 알림을 받는다.

**PlayMode 자동화가 도는 동안 `AssetDatabase.Refresh()`를 부르지 않는다.**
스크립트가 바뀐 상태에서 Refresh 하면 도메인이 리로드되고, `Awake()`에서 잡은
비직렬화 필드(`MaterialPropertyBlock` 등)가 전부 null이 된 채 오브젝트만 살아남는다.
런은 계속 도는 것처럼 보이지만 결과는 오염돼 있다. 컴파일 확인은 런이 끝난 뒤에 한다.

**에디터가 열고 있는 씬 파일을 디스크에서 바꾸면 세션이 죽는다.** 다음 에셋 리프레시에
"외부에서 수정됨 / Reload·Ignore" 모달이 뜬다. 모달은 Unity 메인 스레드를 잡으므로
이후 MCP 호출은 전부 120초 타임아웃까지 매달린다. 프로세스는 멀쩡히 살아 있고 그냥
막혀 있을 뿐이라 생존 확인으로는 못 잡는다. **오토클릭 장치는 없다.**

모달이 떴다면 **사람이 눌러야 한다.** 눌러야 할 것은 둘뿐이다.

| 모달 제목 | 누르는 버튼 | 이유 |
|---|---|---|
| `The open scene(s) have been modified externally` | `Reload` | 디스크 쪽이 의도한 내용이다 |
| `Scene(s) Have Been Modified` | `Save` | 곧 같은 씬을 다시 연다. `Don't Save`는 편집분을 조용히 버린다 |

**이 표에 없는 모달은 누르지 않는다.** Unity는 "에셋을 삭제할까요?"에도 같은 모달
기구를 쓴다. 모르는 제목이면 버튼 목록만 보고하고 손대지 않는다.

---

# 브랜치·커밋·푸시

- `main`에서 직접 장시간 자율 작업을 하지 않는다. 전용 `agent/<description>` 브랜치를 쓴다.
- 자율 작업 중 복구 가능한 마일스톤마다 로컬 커밋할 수 있다.
- 테스트가 실패한 상태를 저장해야 한다면 커밋 메시지와 완료 보고에 명시한다.
- 원격 푸시와 PR 생성은 사용자가 요청한 경우에만 수행한다.
- 푸시를 요청받아도 `main`에 직접 반영하지 않고 기본적으로 Draft PR을 만든다.
- `Library/`, `Temp/`, `Logs/`, `UserSettings/`, `Captures/`는 `.gitignore` 대상이다.
- `Packages/packages-lock.json`은 **반드시 커밋한다** — 다른 기기에서 패키지 버전이
  동일하게 재현되는 근거다.
- 폰트 아틀라스(`* SDF.asset`)를 무관한 커밋에 섞지 않는다 — 열기만 해도 리베이크된다.

---

# 완료 보고

최소 다음을 포함한다.

구현·미구현 요구사항 · Definition of Done 대조표 · 변경 파일 목록 · 테스트 결과와
실패 항목 · 재현 시드 · 콘솔 오류 · 성능 측정 · 고정 캡처 세트 · 실행 가능한 빌드 또는
빌드 차단 원인 · 남은 위험과 가정과 사용자 승인 항목 · 다음 세션의 구체적인 시작점.
