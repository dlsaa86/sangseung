# 상승 (sangseung)

모바일 플레이테스트 빌드.

플레이: https://dlsaa86.github.io/sangseung/

- `index.html` — 모바일 화면 맞춤 래퍼 (게임 본체를 iframe으로 로드, 화면 크기 자동 스케일)
- `sangseung_proto.html` — 게임 본체 (단일 HTML 빌드)

## v14 (2026-08-20)

**게임**
- 눈 축 배선 복구 — 눈 배수 ×1.5 이상이면 줄이 눈을 갈지 않는다. 대신 문턱 상한이 ×3 → ×9로 풀린다
- 눈 동시 상한 3 → 8
- 재발동 계열이 층 경계에서 버려지던 것 수정
- 초기 동전 14 → 6 (상점 4칸 중 2개만 살 수 있다 — "고른다"가 성립)

**UI**
- 상단 레일을 「보유」/「상점」으로 분리 (금테·값표 없음 = 내 것 / 회색테·닢 표시 = 살 것)
- 게이지를 3덩어리로 묶음: 이번 층 / 내가 쌓은 것 / 자원·참고
- 눈 손익판에 갈림 상태 실시간 표시

**결함 수정**
- 상점에서 부품을 사도 화면이 갱신되지 않던 것 (`rejudge` 스코프 오류로 `render()`가 실행되지 않았다)
- 측정 다이얼 23곳이 0을 무시하던 것 (`DIAL.x || 기본값`)
- 시뮬레이터에 인터폰이 통째로 빠져 있던 것 — 거래 22종이 모든 밸런스 측정에서 누락돼 있었다
