# 상승 (sangseung)

모바일 플레이테스트 빌드 **+ 해븐스 헝거 v1(슬롯 시대) 아카이브**.

> **2026-08-26 — 이 저장소는 아카이브 저장소를 겸한다.**
> `dlsaa86/HEAVEN-S-HUNGER` 가 거울 결투 코어로 완전 리뉴얼되면서, 그 이전 작업물 전체(브랜치 9개 · 추적 파일 3,222개)가 `archive/hh-v1/*` 브랜치로 옮겨왔다.
> 무엇이 어디에 있고 어떻게 되돌리는지는 **[ARCHIVE.md](ARCHIVE.md)** 참조.
> 아래 웹 프로토타입(v1/v2)도 삭제가 아니라 **동결** 상태다 — 새 코어가 손에서 실패하면 돌아올 자리다.

플레이: https://dlsaa86.github.io/sangseung/

- `index.html` — 모바일 화면 맞춤 래퍼 (게임 본체를 iframe으로 로드, 화면 크기 자동 스케일)
- `sangseung_proto.html` — 게임 본체 (단일 HTML 빌드)
- `slot_proto.html` — **신 코어 v0** (5x3 슬롯 덱빌딩, balance/ CSV를 직접 읽음): https://dlsaa86.github.io/sangseung/slot_proto.html

## v14 (2026-08-20)

**게임**
- 눈 축 배선 복구 — 눈 배수 ×1.5 이상이면 줄이 눈을 갈지 않는다. 대신 문턱 상한이 ×3 → ×9로 풀린다
- 눈 동시 상한 3 → 8
- 재발동 계열이 층 경계에서 버려지던 것 수정
- 초기 동전 14 → 6 (상점 4칸 중 2개만 살 수 있다 — "고른다"가 성립)

**UI**
- 상단 레일을 「보유」/「상점」으로 분리 (금테·값표 없음 = 내 것 / 회색테·닢 표시 = 살 것)
- 게이지를 3덩어리로 묶음: 이번 층 / 내가 쌓은 것 / 자원·참고
- 눈 손익판에 갈림 상태 실시간 표시

**결함 수정**
- 상점에서 부품을 사도 화면이 갱신되지 않던 것 (`rejudge` 스코프 오류로 `render()`가 실행되지 않았다)
- 측정 다이얼 23곳이 0을 무시하던 것 (`DIAL.x || 기본값`)
- 시뮬레이터에 인터폰이 통째로 빠져 있던 것 — 거래 22종이 모든 밸런스 측정에서 누락돼 있었다

## balance/ — 신 코어 밸런스 데이터 (RULESET 2.0)

유니티 이행 전 슬롯 코어(5x3·전력·눈)의 수치 정본. 편집은 Google Sheets, 이력은 이 폴더.
자세한 규약·검증 체크리스트는 `balance/BALANCE_README.md`.

- 심볼 10 · 승객 23 · 설비 161 · 인터폰 거래 24 · 연쇄 사전 · 7734층 진행 규칙
- 검증 하네스: `balance/balance_sim.js`(코어) + `balance_sim_v2.js`(전 구조)
