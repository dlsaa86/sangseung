# Assumption Log

에이전트가 작업을 멈추지 않기 위해 사용한 임시 기본값을 기록한다. 승인되지 않은 가정을 최종 결정처럼 취급하지 않는다.

## 기록 형식

```markdown
## A-YYYYMMDD-NN — 제목

- 상태: Active | Confirmed | Replaced | Invalid
- 작성일:
- 관련 요구사항:
- 사용한 가정:
- 선택 이유:
- 구현 위치:
- 교체 방법:
- 사용자 확인 필요 여부:
```

---

## A-20260730-01 — 기준 하드웨어 프로파일 미완성

- 상태: Active
- 작성일: 2026-07-30
- 관련 요구사항: 성능 완료 조건
- 사용한 가정: 현재 알려진 기준 장비인 Ryzen 7 5700 / RTX 3070을 임시 기준으로 사용한다.
- 선택 이유: CPU와 GPU 정보는 있으나 RAM, Windows 빌드, 그래픽 API, 품질 프리셋이 고정되지 않았다.
- 구현 위치: 향후 `TargetHardwareProfile`
- 교체 방법: 실제 테스트 PC 정보를 입력하고 동일 캡처·프로파일링을 다시 수행한다.
- 사용자 확인 필요 여부: 성능 완료 선언 전 필요

## A-20260730-02 — 초기 밸런스 값은 검증용 임시값

- 상태: Active
- 작성일: 2026-07-30
- 관련 요구사항: 정상 영혼, 흡수체, 증식체, 계약, 요구 전력
- 사용한 가정: 현재 `PrototypeConfig.asset`의 값 또는 기술 부록의 임시값을 사용한다.
- 선택 이유: 최종 수치 밸런스는 프로토타입 제외 대상이다.
- 구현 위치: `Assets/Prototype_Elevator/Data/PrototypeConfig.asset`
- 교체 방법: 코드 변경 없이 데이터 에셋 또는 프로파일만 수정한다.
- 사용자 확인 필요 여부: 최종 밸런스 단계에서 필요

## A-20260730-03 — 승인 대기 비주얼은 플레이스홀더 유지

- 상태: Active
- 작성일: 2026-07-30
- 관련 요구사항: 캐릭터, 승객, 모션, 색·재질, 공포 강도
- 사용한 가정: 최종안을 만들지 않고 비교 가능한 2~3개 프리셋 또는 교체 가능한 플레이스홀더를 사용한다.
- 선택 이유: 코어 플레이 검증 전에 최종 아트 결정을 잠그지 않기 위해서다.
- 구현 위치: 시각 프로파일, 머티리얼, 프리팹, 데이터 정의
- 교체 방법: 데이터 참조 또는 프리팹 참조 교체
- 사용자 확인 필요 여부: Phase 6에서 필요

## A-20260730-04 — 첫 세션은 1층에서 전체 흐름 검증

- 상태: Confirmed
- 작성일: 2026-07-30
- 관련 요구사항: `CURRENT_PHASE.md`
- 사용한 가정: 10층 확장 전에 1층 Hero Slice에서 계약부터 과수확 결과까지 연결한다.
- 선택 이유: 핵심 재미와 판독성을 먼저 검증하기 위해서다.
- 구현 위치: 현재 프로토타입 씬과 런/층 컨트롤러
- 교체 방법: Gate A~D 통과 후 데이터 기반 10층 흐름으로 확장
- 사용자 확인 필요 여부: 확인됨

## A-20260730-05 — 에디터가 올린 패키지 버전을 그대로 가져감

- 상태: Active
- 작성일: 2026-07-30
- 관련 요구사항: `TECH_SPEC.md` §1 "패키지 버전을 에이전트가 임의로 변경하지 않는다"
- 사용한 가정: 세션 시작 시 작업 트리에 `com.unity.ai.assistant` 2.16.0-pre.1 → 2.17.0-pre.1
  변경이 이미 있었다. 에이전트가 만든 변경이 아니라 **에디터가 자체 수행한 것**이므로
  되돌리지 않고 브랜치에 가져간 뒤 별도 커밋(`7034b59`)으로 명시했다.
- 선택 이유: 되돌리면 현재 실행 중인 에디터와 MCP 경로가 깨질 수 있었다. 또 `packages-lock.json`은
  항상 재현 가능한 상태로 커밋해야 한다(`CLAUDE.md`).
- 구현 위치: `Packages/manifest.json`, `Packages/packages-lock.json`
- 교체 방법: 사용자가 2.16.0-pre.1을 원하면 두 파일을 되돌리고 에디터를 재시작한다.
- 사용자 확인 필요 여부: **필요.** 패키지 버전 변경은 승인 사항이다.

## A-20260730-06 — 개발 기기가 문서의 기준 하드웨어가 아니다

- 상태: Active
- 작성일: 2026-07-30
- 관련 요구사항: `TECH_SPEC.md` §13 성능 기준, A-20260730-01
- 사용한 가정: 이번 세션의 실제 개발·측정 기기는 **Apple M5 / Metal / macOS 26.5.2 / 24GB**다.
  A-20260730-01이 가정한 Ryzen 7 5700 / RTX 3070 / Windows가 아니다.
- 선택 이유: 기기를 고를 수 없었다. §13이 "기준 PC 미지정 상태에서는 성능 완료를 선언하지
  않는다"고 하므로, 측정은 수행하되 **성능 완료를 선언하지 않는다.**
- 구현 위치: `Logs/heroslice_perf.txt`, `Captures/HeroSlice/manifest.txt`
- 교체 방법: 기준 PC에서 `Ascend/Measure Hero Slice Performance`와
  `Ascend/Capture Hero Slice Set`을 다시 실행한다. 캡처 베이스라인도 기기 종속이므로 새로 세운다.
- 사용자 확인 필요 여부: 성능 완료 선언 전 필요

## A-20260730-07 — Hero Slice 요구 전력 460

- 상태: Active
- 작성일: 2026-07-30
- 관련 요구사항: `CURRENT_PHASE.md` §1 "요구 전력 달성 후 확정 또는 과수확 선택"
- 사용한 가정: 1층 요구 전력 460, 스핀 5회. 400시드 헤드리스 측정에서 무계약 기준
  5스핀 내 달성률 80%, 달성 스핀 중앙값 3 — 절반 이상이 스핀 2회를 남긴 채 요구 전력을 넘긴다.
- 선택 이유: 남는 스핀이 곧 과수확 선택지다. 너무 낮으면 1스핀에 끝나 선택이 생기지 않고,
  너무 높으면 5스핀을 다 써도 못 넘겨 선택 자체가 없다. 추측이 아니라 측정으로 골랐다.
- 구현 위치: `Spin/FloorPlan.cs`의 `PrototypeCurriculum.HeroSlice.RequiredPower`
- 교체 방법: 그 필드 하나만 고친다. 코드 구조 변경 불필요.
- 사용자 확인 필요 여부: 최종 밸런스 단계에서 필요

## A-20260730-08 — 위험 점수 가중치와 히스테리시스 임계값

- 상태: Active
- 작성일: 2026-07-30
- 관련 요구사항: `MASTER_PRD.md` §7·§9, `TECH_SPEC.md` §6.4
- 사용한 가정: 흡수체 1.0 / 증식체 1.2 / 과수확 3.2 / 과적 3.0 / 스핀소진·미달 4.0,
  Warning 진입 3.0·이탈 2.0, Critical 진입 7.0·이탈 5.5.
- 선택 이유: **과수확 가중치만은 임의값이 아니다.** `MASTER_PRD.md` §7이 과수확을
  "공간적 사건"으로 규정하므로 1회만으로 Warning에 진입해야 한다(≥ WarningEnter).
  실제로 처음 잡은 2.6에서는 PlayMode 검증이 "과수확 1회 후에도 Stable"로 실패했고,
  그래서 3.2로 올린 뒤 불변식을 테스트로 고정했다. 나머지 값은 상대적 비중만 맞춘 임시값이다.
- 구현 위치: `Risk/RiskEvaluator.cs`의 공개 필드
- 교체 방법: 필드값 수정. 단 `OverharvestWeight >= WarningEnter` 불변식은 테스트가 지킨다.
- 사용자 확인 필요 여부: 최종 밸런스 단계에서 필요

## A-20260730-09 — 공포 표현 강도는 3종 프리셋으로 유지

- 상태: Active
- 작성일: 2026-07-30
- 관련 요구사항: `VISUAL_SPEC.md` §11, `MASTER_PRD.md` §1
- 사용한 가정: `RiskIntensity.Restrained / Standard / Heavy` 3종. 현재 기본값 Standard.
  조명 밝기·색, 깜빡임 속도·깊이, 경고등, 물체 진동, 카메라 흔들림(≤0.005m), 험 음량·피치가
  단계마다 함께 움직인다. Collapse에서도 암전하지 않는다(§6 "정보를 숨기지 않는다").
- 선택 이유: 최종 공포 강도는 승인 대기 항목이라 하나로 잠그지 않는다.
- 구현 위치: `Risk/RiskProfile.cs`의 `Preset(RiskIntensity)`, `RiskStateView._intensity`
- 교체 방법: 인스펙터에서 `_intensity` 변경, 또는 런타임 `RiskStateView.SetIntensity`.
  비교 캡처는 같은 시드·좌표로 `Ascend/Capture Hero Slice Set`을 프리셋마다 돌린다.
- 사용자 확인 필요 여부: **필요.** Phase 6 승인 게이트 대상.

## A-20260730-10 — 연출 템포 3종 프리셋

- 상태: Active
- 작성일: 2026-07-30
- 관련 요구사항: `MASTER_PRD.md` §6 "제거 → 빈칸 → 신규 심볼 유입을 시각적으로 생략하지 않는다"
- 사용한 가정: `SpinPresenter.Tempo.Readable / Standard / Brisk`. 기본 Standard
  (통관 공개 0.32s, 판독 정지 0.45s, 정화 맥동 0.55s, 빈칸 유지 0.30s, 재충전 유지 0.40s).
  연쇄가 깊어질수록 0.86배씩 빨라지되 0.35배 아래로는 내려가지 않는다.
- 선택 이유: 20연쇄가 순식간에 지나가면 무한 루프 방지가 걸렸다는 사실 자체를 못 본다.
  반대로 느리면 5스핀이 지루해진다. 사람 플레이테스트 전까지는 잠정값이다.
- 구현 위치: `View/SpinPresenter.cs`
- 교체 방법: 인스펙터 `_tempo` 변경. 개별 값도 전부 필드다.
- 사용자 확인 필요 여부: 사람 플레이테스트 후 필요
