# CURRENT_STATE — 구현 현황과 Notion 대비 Gap

> **작성**: 2026-08-04 10:1x · 브랜치 `agent/phase2-full-prototype` @ `0a09ea1`
> **방법**: 코드 직독(파일:줄번호) + 씬 YAML grep + 로그 실측. 읽기 전용 조사.
> **상위**: [PROJECT_VISION.md](PROJECT_VISION.md)

> ⚠ **이 문서는 스냅샷이다.** 조사 중 **다른 세션이 동시에 이 워킹 트리에 쓰고 있었다**
> (10:02~10:0x, `RemainingSpinSettlementProfile.cs` · `SettlementTests.cs` · `FloorPlan.cs` ·
> `ASSUMPTION_LOG.md` · `PENDING_DECISIONS.md`). 아래 수치 중 일부는 이미 낡았을 수 있다.
> 판단 전에 `git status`와 파일 mtime을 다시 확인할 것.

---

## 1. 한 줄 요약

**병목은 로직이 아니라 판독성과 배선이다.**

핵심 게임플레이 16개 항목이 **전부 코드로 구현돼 있고**, EditMode 556 PASS / 0 FAIL,
10층 플레이 가능, Windows 빌드 산출물이 존재한다.
막고 있는 것은 **화면에서 안 읽힌다**는 것 하나다.

## 2. 규모

| 항목 | 값 |
|---|---|
| C# 파일 | 208 (공개 타입 309) |
| 에디터 툴 | 37 |
| 씬 GameObject | 603 |
| EditMode 테스트 | 556 PASS / 0 FAIL (`Logs/selftest_v6.txt`, 08-04 03:34) |
| 백로그 Required | 129 (VERIFIED 84 / CONNECTED 38 / OUT_OF_SCOPE 5 / SKELETON 2) |
| 고정 캡처 | 24장 + manifest (`Captures/TenFloor/`) |
| Windows 빌드 | 134.9 MB (08-01), Dev 빌드 206.8 MB (08-02) |

## 3. 구현된 기능 (코드 실측)

| # | 항목 | 판정 | 핵심 근거 |
|---|---|---|---|
| 1 | 3×3 스핀 + 결정론적 시드 | ✅ | `SpinBoard.cs:15-17`, `SpinSeed.cs:29-44` (murmur3, `Derive(runSeed,floor,spinIndex)`) |
| 2 | **인접** 3개 이상 정화 | ✅ | `SpinEngine.cs:413-414` `FindConnectedComponent`, `SpinRuleSet.cs:91` `RequireAdjacencyToPurify=true` |
| 3 | 직선 3개 보너스 | ✅ | `SpinBoard.cs:125-138` 직선 8종 테이블 |
| 4 | 4개 직교 연결 + 캐스케이드 + 캡 20 | ✅ | `SpinEngine.cs:468-507` BFS, `:394` `ClusterMinimumSize=4`, `SpinRuleSet.cs:114` `MaxCascadeDepth=20` |
| 5 | 잔류 효과 | ⚠ 설계 결함 1건 | `SpinEngine.cs:375-391`. §6.4 참조 |
| 6 | 계약 2종 | ✅ | `FloorSession.cs:431-443`, `SpinRuleSet.cs:207-217` |
| 7 | 전력 임계 8구간 | ✅ | `PowerThresholds.cs:31-50` |
| 8 | **2단 통합 레버** | 🔴 **미구현** | §6.1 참조 |
| 9 | 잔여 스핀 현금화 | ⚠ 에셋 미생성 | §6.2 참조 |
| 10 | 승객·부품 최소 4종 | ✅ 초과 (11종) | `BuildLoadout.cs:159-279` |
| 11 | 과적·무게·요구 전력 | ✅ | `WeightProfile.cs:159-165` |
| 12 | 위험 4단계 | ✅ | `RiskLevel.cs:9-33`, `RiskEvaluator.cs:145-179` (히스테리시스) |
| 13 | 승객 반응 | ✅ 씬 배선 완료 | `PassengerReactionDirector.cs:243-301` |
| 14 | 사고 기록기 | ✅ | `AccidentRecorder.cs:56-103`, `RunSummaryBuilder.cs:42-122` |
| 15 | 1인칭 이동·상호작용 | ✅ | `FirstPersonController.cs`, `CrosshairInteractor.cs:178-213` (SphereCast) |
| 16 | 10층 진행 | ✅ | `FloorPlan.cs:135-136`, `RunSession.cs:341-438`. 씬 `_mode:1`(TenFloor) |

### 3.1 결정론 — 요구 충족

- 판정 경로에 `UnityEngine.Random` **없음**. `System.Random` 단독
- 시드는 **좌표 기반**이라 "N층 M번째 스핀" 단독 재현 가능
- 인덱스 `column*Rows+row`로 통일

## 4. 부분 구현 / 임시 에셋

| 항목 | 상태 |
|---|---|
| `RemainingSpinSettlementProfile.asset` | **존재하지 않음.** 씬에 `_settlementProfile` 키도 없음 → 런타임은 항상 코드 프리셋 |
| `PendingSettlementMoney` | 계산은 되지만 **화면 소비처 0곳** |
| `PowerBand.Damaged` | 정의는 있으나 **소비처 0곳** (`UP-POWER-02`가 CONNECTED인 이유) |
| `TargetHardwareProfile` | 데이터 슬롯만 있고 **사양 미지정** → 기술 부록 08이 *"미지정 상태에서는 성능 완료를 선언하지 않는다"*고 규정 |

## 5. 작동하지 않는 것 / 기술 부채

### 5.1 레거시 병렬 경로

`Scripts/Core/`(7파일)와 `Scripts/Roulette/`(4파일)은 **죽은 경로**다.
씬이 `_mode:1`(TenFloor)이라 라이브 경로는 `Run/` + `Spin/`이고,
`Roulette/RouletteController`는 공 3개 릴 구조로 **3×3 스핀과 무관**하다.
`UP-TEST-11`(미사용 레거시 정리)이 SKELETON인 이유.

### 5.2 백로그 문서 자체의 모순

- §1은 `PASS_3: COMPLETE`라고 적지만, Pass 3 완료 조건인 `PASS3_GATED` 28항목 중
  **VERIFIED는 3건**이고 `VISUAL_VERDICT.md`는 **연속 REJECT**다. **성립하지 않는다.**
- 상태값을 §0.3은 7개로 정의하는데 §0.2가 8번째(`OUT_OF_SESSION_SCOPE`)를 추가한다
- 게이트가 보고하는 "미달 40건"은 실제 **45건**이다 (오디오 `OUT_OF_SESSION_SCOPE` 5건 제외한 수)
- §6 통계표의 수정 백로그 "22건"은 낡았다 — 실제 고유 **59건**

## 6. 🔴 가장 위험한 문제

### 6.1 2단 통합 레버 미구현 — PRD §7 확정 위반

현재는 **분리된 3개 오브젝트**다:

| 기능 | 구현 |
|---|---|
| 일반 스핀 | `InteractableLever.cs:39` → `RouletteInteractionBridge.cs:228` |
| 과수확 | `InteractableOverharvestLever.cs:75` → `:245` |
| 전력 확정 | `InteractablePowerTank.cs:29` → `:264` |

**유지 입력은 전 소스에 0곳.** `Interact()`가 즉시 발동한다.
`LeverStateMachine`의 8단계는 애니메이션 단계이지 걸림점이 아니다 (걸림점은 `Latched` 하나).

> ⚠ 더 나쁜 것: 코드 주석이 §7 확정 **이후에도** *"실행 레버가 추가 스핀을 겸하지 **않는** 것이
> 핵심이다(§7)"*라고 정반대로 인용하며 미구현을 정당화하고 있었다.
> 이번 미커밋 변경이 그 주석을 정정했다.

### 6.2 과수확이 "실제 고민"으로 작동하지 않는다 — DoD 구조적 미성립

PRD §7은 선택 전에 **현재 전력 / 남은 스핀과 즉시 획득할 돈 / 캐스케이드 배수 /
잔류 대가 / 실패 시 예상 손실 / 다음 임계점**을 표시할 것을 요구한다.

`PendingSettlementMoney`가 화면에 없으므로 **플레이어는 무엇을 포기하는지 모른 채 선택한다.**
DoD 13.3 「과수확이 실제 고민으로 작동함」이 구조적으로 성립할 수 없다.

### 6.3 심볼 3종이 구분되지 않는다 — 판독성 4.0이 산술적으로 불가능

독립 평가자 원문:
> 9칸이 전부 **"회색 원반 위 붉은 각진 덩어리 하나"**이고 **회색조로 바꾸면 구분이 완전히 사라진다.**

`VISUAL_SPEC` §4는 색 외 최소 3항목이 달라야 한다고 요구한다.
심볼 항목과 전력 판독 항목이 2점인 한 **평균 4.0은 산술적으로 불가능하고,
셸을 아무리 고쳐도 2.6대에서 멈춘다.** → `UP-FIX-82`

**해결 재료는 이미 있다.** [ART_BIBLE.md](ART_BIBLE.md) §7.2에 흡수체(중심으로 빨려드는 코어·수렴 홈)와
증식체(분열선·새싹 돌기·다핵)의 형태 언어가 확정돼 있다. **발명이 아니라 이식이다.**

### 6.4 증식체 잔류가 대가가 아니라 이득이다

증식체 계약 보유 시 잔류 페널티가 오히려 유리하게 작동한다.
`ResidualPenaltyMultiplier`를 2.0 → 3.2로 **올렸더니** 7·8·9층 클리어율이
90/97/100 → **100/100/100**이 됐다. *"이름이 하는 말과 코드가 하는 일이 다르다."*
→ `PENDING_DECISIONS.md` `P-20260804-03`

### 6.5 계기판·표찰이 화면에 아예 없다

**계기판 5개 + 계약 표찰 3개가 네 포즈 전부에서 0픽셀.**
안쪽을 향하고 있지 않고(단면 재질) `PanelBack`/`ContractPanel`에 가려 있다. → `UP-FIX-87`

### 6.6 레버 앞에서 결과판이 사라진다

`04_lever_join`(레버를 당기려고 선 자리)에서 아홉 창 전부가 불투명 회녹색 원반,
붉은 화소 **0.02%**. 원거리에서는 0.22%.
**예각+근거리에서 유리 프레넬 스페큘러가 판을 통째로 덮는다.**

> *"레버를 당겨 판을 해결하는 게임인데 레버 앞이 판이 안 보이는 유일한 자리."*

## 7. 씬 상태

배치는 사실상 완료돼 있다 — 공간 셸 / 가위문(`LinkPivot_0~35`) / **9실 통관 캐비닛**
(`SoulWindowModule_00~22` 9개가 3×3에 대응, `Cell_0/1/2`가 `Tube_0/1/2`의 자식) /
레버 2개 / 전력 탱크 / 계기판 / 계약 표찰 / 사고 프린터 / 소품 / 외부 로비·샤프트.

배선 (`Prototype_Elevator.unity:25265-25275`):
`_mode:1`, `_seed:1337`, `_overharvestProfile`·`_weightProfile`·`_spinBalanceProfile`·
`_floorCurriculum`·`_contractProfile` 배선됨. **`_settlementProfile` 키 없음** (신규 필드, 씬 미재직렬화).

⚠ **레버가 `ExecutionLever` + `OverharvestLever` 두 개로 물리적으로 분리돼 있어 PRD §7과 다르다.**

## 8. 테스트

| 로그 | 결과 |
|---|---|
| `Logs/selftest_v6.txt` (08-04 03:34) | **556 PASS / 0 FAIL** |
| `Logs/editmode_tests.txt` (08-04 04:18) | **537 PASS / 0 FAIL** |

⚠ 두 로그 모두 **미커밋 변경 이전**이다. 신규 회귀 테스트 3건은 실행 기록이 없다.

### 8.1 성능 (에디터 Play 모드 측정 — 빌드 성능 아님)

`Logs/render_budget.txt` (08-03, RTX 3070 / 1080p):
드로우콜 중앙 1061 / 최악 1327 · SetPass 중앙 98 · 삼각형 중앙 33,421
프레임타임 중앙 8.34ms / **p95 16.65ms (예산 11.11ms 초과)** / 최악 1682ms

## 9. 시각 평가 — 최신 REJECT

**2026-08-04, `Captures/cabin_v5_20260804` — VERDICT: REJECT**

1. **판독성 평균이 2.67 → 2.67로 전혀 움직이지 않았다.** 개선된 것은 스타일 축(3.50→4.00)뿐
2. **금지 #17(지속적 색수차)이 새로 켜졌다** — 훼손 대상이 하필 상호작용 우선순위 1위인
   「실행」·「과수확」 표찰 두 개 (파랑 획/붉은 획으로 갈라져 글자로 안 읽힘)
3. `C_toward_gate`에서 가위문 손잡이·하부 롤러·프레임 **전부 소실**
4. 장치 캐비닛 명도가 그늘진 벽과 같아짐 — 3×3을 붙들어 주는 것이 켜진 포트홀 9개뿐

> **단 이 REJECT는 "이전 빌드로 되돌려라"가 아니다.**
> 되돌릴 대상은 post 설정과 명도 배분이지 셸·조명·재질이 아니다.
> 레퍼런스 근접도만 보면 새 빌드가 명확히 앞선다.

**v6~v8은 독립 평가가 없다** — 캡처는 있으나 평가자가 보지 않았다.

**`Captures/baseline.txt`가 없다** = 승인된 베이스라인이 부재하다 (`UP-FIX-84`).

## 10. Notion 기획과 현재 구현의 차이

### 10.1 미충족 3건

| PRD 요구 | 상태 |
|---|---|
| §7 2단 통합 레버 + 0.7~1.0초 유지 입력 | 🔴 미구현 |
| §7 선택 전 예상 정산 가치 표시 | 🔴 미구현 |
| DoD 심볼 3종 1초 내 구분 | 🔴 미충족 |

### 10.2 Notion 문서 부채 (구현이 아니라 문서가 낡음)

**참고 원본 4개가 폐기된 정화 규칙을 아직 적고 있다** — 참고 00(도파민 #2) /
참고 01(「개수 안전망」 절 + "3개 흩어짐" 패턴 행) / 비주얼 부록 07(VFX 규칙) / 99 결정 로그.
**MASTER PRD §6.1은 최신이다.** Notion을 다시 참조할 때 이 함정에 빠지지 말 것.

**에셋 부록 06 §13 장치 프롬프트에 폐기된 정지 버튼 3구절**이 남아 있다.

### 10.3 명단 충돌

승객 로스터가 세 벌이고 이름이 하나도 겹치지 않는다
(티켓 T-04 5종 / 기술 부록 08 4종 "가칭" / 핵심 개편 7명).
**핵심 개편 7명이 정본** → [DECISION_LOG.md](DECISION_LOG.md) C-01.

현재 코드의 11종(`PSG_SURVEYOR` 등)은 **세 명단 중 어느 것과도 다르다.**

## 11. 미커밋 변경

16건 (조사 시점). 코드 8 + 문서 3 + 신규 5.

**결함 수정 2건**
- `FloorSession`의 `_settlement`를 **생성자 주입 `readonly`**로 전환 —
  직전엔 대입하는 코드가 **어디에도 없어** 프로파일을 만들어도 한 자리도 안 바뀌었다
- 과수확 레버 덮개 판정을 `f.CanTakeExtraSpin` 하나로 통일 —
  직전엔 임계값을 올려도 100%에서 열리고, `MaxExtraSpins=0`이어도 열렸으며,
  **열린 레버를 당기면 조용히 거부**했다

**신규 하네스**: `Assets/Editor/BalanceSweep.cs` (827행) — 시드 300개 × 정책 3종 스윕,
요구 전력 곡선 역산. 기존 프로브가 **"요구 전력을 채웠는데 스핀이 남았다"는 국면 자체를
만들지 않아** 이 게임의 대표 장면이 측정되지 않고 있었다.

**신규 기준**: `docs/runtime/FUN_CRITERIA.md` — 「재미」의 조작적 정의 6개 지표군.

### 11.1 밸런스 산출물의 미적용

- 역산된 요구 전력 곡선(`215/430/…/1670`)이 코드(`350/355/…/875`)에 **미반영**
- `FloorCurriculumProfile.asset`의 3층 배율 **1.9**와 코드 **1.8**이 어긋난 채 씬에 배선 →
  **스윕이 잰 것과 게임이 도는 것이 3층에서 다르다**
- 두 밸런스 리포트가 서로 다른 상태를 재고 있다

> ⚠ **정정**: 정산율은 조사 직후 **다른 세션이 0.05/0.20 → 0.15/0.60으로 적용**했다
> (근거: 옛 값에서 기대값 최적 플레이가 과수확을 91.7% 선택 = 선택이 아니었음).
> 이 항목은 해소됐다.

## 12. 다음 작업 후보 (우선순위)

### P0

1. **심볼 3종 실루엣 분리** (`UP-FIX-82`) — 판독성 4.0의 산술적 전제. 재료는 ART_BIBLE §7.2에 있다
2. **계기판·표찰 0픽셀 수정** (`UP-FIX-87`) — 법선 방향과 가림
3. **레버 앞 결과판 소실** — 유리 프레넬 각도별 억제
4. **2단 통합 레버 구현** — PRD §7 확정 위반

### P1

밸런스 역산 곡선 적용 / `FloorCurriculumProfile` 에셋↔코드 일치 /
증식체 잔류에 위험 축 추가 / `Captures/baseline.txt` 수립 /
Notion 참고 원본 4개 정화 규칙 갱신 / 3D 프롬프트 정지 버튼 3구절 삭제 /
`RemainingSpinSettlementProfile.asset` 생성 + 씬 배선.

### 반복 상한 도달 — 4번째 시도 금지

`UP-FIX-36`(**7라운드 연속 지적**) / `UP-FIX-02`(3회) / `UP-FIX-19`(3회, 배치 결정 대기) /
`UP-FIX-25`(3회, 악화). `visual-verify` §6에 따라 **구조 변경으로 간다.**

## 13. 사용자 결정 대기

| ID | 내용 |
|---|---|
| C-02 (신규) | **태그·시너지를 화면에 표시할 것인가** → [PASSENGER_SYSTEM.md](PASSENGER_SYSTEM.md) §12.3 |
| `P-20260804-01` | 2단 통합 레버 — 권고: 과수확에만 유지 입력 추가 |
| `P-20260804-02` | 예상 정산 가치 화면 부재 — `UP-FIX-87`과 함께 처리 |
| `P-20260804-03` | 증식체 잔류에 위험 축 없음 — 권고: `RiskEvaluator` 수정 |

## 14. 운영 위험

🔴 **동시 쓰기.** 조사 중 다른 세션이 같은 워킹 트리에 쓰고 있었고, Unity 에디터가 열려 있다.
`CLAUDE.md`가 금지하는 상태다 — `.unity`/`.prefab`/`.mat`/`.asset` 손상은 조용히 일어나고
되돌리기 어렵다. **작업 전에 단일 소유자를 확정할 것.**
