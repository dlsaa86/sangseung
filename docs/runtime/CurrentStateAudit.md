# 현재 상태 감사 — 2026-08-01

> 작성 시점: 2026-08-01 (세션 시작 직후)
> 방법: 저장소·Unity 에디터·로그를 **직접 관측**한 것만 기록한다. 이전 세션 문서의 주장은
> 재확인한 것만 옮긴다. 재확인하지 않은 것은 "미확인"으로 표시한다.

## 0. 작업 트리와 브랜치

| 항목 | 값 |
|---|---|
| 브랜치 | `agent/phase2-full-prototype` |
| `main` 대비 | **22 커밋 앞섬**, 0 뒤처짐 |
| 마지막 커밋 | `6dea2a1` (08-01 03:02) "함수가 안 돈 게 아니라 어셈블리가 컴파일되지 않았다" |
| 미커밋 변경 | 6개 파일 (아래) |

### 미커밋 변경의 의도 분류

원격보다 앞선 로컬 작업이다. **버리지 않는다.**

| 파일 | 분류 | 판단 |
|---|---|---|
| `.claude/hooks/unity-modal-autoclick.ps1` | 도구 개선 | 모달 자동 클릭 화이트리스트 확장. 유지 |
| `.claude/hooks/unity-preflight.sh` | 도구 개선 | 프리플라이트에서 모달 클리너 호출. 유지 |
| `.claude/settings.json` | 도구 개선 | 훅 타임아웃 10초→30초. 위 두 변경의 동반 수정. 유지 |
| `CLAUDE.md` | 문서 | 모달 자동 클릭 규약 문서화. 위 변경과 짝. 유지 |
| `Assets/Prototype_Elevator/Fonts/NanumGothic SDF.asset` | **폰트 아틀라스 재생성** | 4082줄 삭제. Unity가 재임포트하며 아틀라스를 다시 구운 것. 기능 영향 확인 필요 |
| `Assets/TextMesh Pro/.../LiberationSans SDF - Fallback.asset` | 폰트 아틀라스 재생성 | 위와 같은 원인 |

앞의 4개는 커밋 `9623475`에서 시작된 모달 자동화 작업의 마무리분이며 서로 짝을 이룬다.
뒤의 2개는 에디터가 자동 생성한 것이라 의도적 변경이 아니다 —
`.claude/hooks/font-atlas-guard.sh`가 이 상황을 안내하도록 되어 있다.

## 1. 컴파일과 콘솔 — 직접 관측

| 항목 | 결과 | 근거 |
|---|---|---|
| Unity 컴파일 오류 | **0** | `Unity_RunCommand` 컴파일 성공 |
| 콘솔 오류 | **0건** | `Unity_GetConsoleLogs(Error)` → `totalCount: 0` |
| Unity 에디터 | 실행 중 (PID 25868), Edit 모드 | SessionStart 훅 + `Logs/Editor.log` |
| 활성 씬 | `Assets/Prototype_Elevator/Scenes/Prototype_Elevator.unity` | 캡처 리그가 마지막으로 연 씬 |

## 2. 테스트 — 이번 세션에서 재실행

### EditMode (자체 헤드리스 러너, `D-20260730-06`)

**89 PASS / 0 FAIL** — 2026-08-01 재실행. 전문은 `Logs/editmode_tests.txt`.

| 스위트 | PASS | FAIL |
|---|---|---|
| `SpinEngineTests` | 29 | 0 |
| `RunTests` | 16 | 0 |
| `RiskEvaluatorTests` | 11 | 0 |
| `BuildTests` | 33 | 0 |

### PlayMode — **기록이 오래됐다 (재실행 필요)**

`Logs/tenfloor_playmode.txt`는 **07-31 22:19** 산출물이다. 그 뒤 커밋 3개가 들어갔다.

| 커밋 | 시각 | 내용 |
|---|---|---|
| `d97ed3e` | 22:20 | **"판정과 제거를 둘 다 고쳐야 했다"** — 정화 판정 변경 |
| `71e8c0f` | 22:50 | 입구 카메라 위치 |
| `6dea2a1` | 03:02 | 씬 + `TenFloorSceneBuilder` |

`d97ed3e`가 스핀 판정을 건드렸으므로 **현재 코드의 PlayMode 증거가 없다.**
EditMode 89건이 그 변경을 덮지만, 씬 경로(상호작용 전용)는 덮지 않는다.

기록된(오래된) 결과 — 198 PASS / 0 FAIL / 콘솔오류 0:

| 런 | 방문 층 | 결말 |
|---|---|---|
| 보수·1337 | [1,2,3,4,5,6,8] | 8층 Crash |
| 공격·1337 | [1,2,3,5,6,7,8,10] | 완주 |
| 보수·20260731 | [1,2,3,5,6,8,10] | 완주 |
| 공격·20260731 | [1,2,3,5,6,8,10] | 완주 |
| 재현·1337 | [1,2,3,4,5,6,8] | 첫 런과 동일 → 결정론 성립 |

**시드가 2개다** (`TenFloorAutoPilot.cs:122-135` — `seedA` = 런 시드, `seedB` = 20260731).
`P2-Gate D`와 이번 목표는 **고정 시드 3개**를 요구한다. 헤드리스에는
`고정 시드 3개 이상이 10층을 완주한다`(`BuildTests.cs:57`)가 있으나 씬 경로에는 없다.

## 3. 고정 캡처 — 생성됨, **독립 평가 미수행**

`Captures/TenFloor/` 18장 + `manifest.txt`, **2026-08-01 03:02~03:03 생성**.

`docs/runtime/VisualGapAnalysis.md`는 **07-31 21:21** 기록이므로 이 세트를 보지 않았다.
즉 **현재 캡처 세트에 대한 독립 시각 평가가 존재하지 않는다.**

`machineFingerprint`: `Windows|Direct3D12|NVIDIA GeForce RTX 3070|6000.5.5f1` — 이 기기 기준선.

## 4. 이전 문서의 주장 중 **이번에 반증된 것**

`docs/runtime/GapAnalysis.md` 말미는 `PowerBand.Jettison`이
"화물 포기라고 말한 뒤 아무 일도 일어나지 않는다"고 기록했다. **현재 코드에서는 사실이 아니다.**

- `PowerThresholds.cs:106` — `Ascends()`가 `>= Jettison`으로 바뀌어 런이 끝나지 않는다
- `RunSession.cs:265` — `if (result.RequiresJettison) LastJettison = PayJettisonCost();`
- `RunSession.cs:306` — `PayJettisonCost()`가 실제 대가를 물린다
- `BuildTests.cs:55` — `화물 포기 구간은 런을 끝내지 않고 대가를 물린다` 테스트 존재

→ `GapAnalysis.md`의 해당 절은 **오래된 기록**이다. 이 문서가 우선한다.

## 5. 커리큘럼 매핑 — 목표 지시와 구현이 다르다

이번 목표가 요구한 커리큘럼과 `PrototypeCurriculum._tenFloors`(`FloorPlan.cs:160-260`)를 대조한다.

| 층 | 목표 지시 | 구현 (`TeachesRule`) | 일치 |
|---|---|---|---|
| 1 | 조작 / 기본 정화 | 1인칭 이동·조준·자동 스핀·기본 전력 | 부분 (정화가 2층으로 분리) |
| 2 | 직선 | 개수 안전망 — 3개 기본 정화 | **불일치** |
| 3 | 연결·캐스케이드 | 직선 3개 패턴 배수 | **불일치** |
| 4 | 계약 | 4개 직교 연결 → 첫 캐스케이드 | **불일치** |
| 5 | 빌드 선택 | 승객·부품 보상과 빌드 방향 선택 | 일치 |
| 6 | 증식체 | 흡수체 계약 | **불일치** |
| 7 | 계약 비교 | 증식체와 잔류 가중치 이월 | **불일치** |
| 8 | 적재 압박 | 계약 비교 + 적재 무게로 요구 전력 증가 | 부분 |
| 9 | 과수확 | 푸시 유어 럭 | 일치 |
| 10 | 종합 시험 | 새 규칙 없음 — 종합 시험 | 일치 |

구현은 "1층 조작 / 2층 기본정화"로 쪼갠 뒤 2~4층이 한 칸씩 밀렸고, 6·7층의
계약↔증식체 순서가 목표 지시와 반대다. 구현 주석은 근거를 **"노션 99 10층 테스트 구조"**로
든다. 어느 쪽이 최신 원본인지 확인이 필요하다 → 독립 요구사항 감사에 위임.

## 6. 실제 런에서 **층이 건너뛰어진다**

관측된 방문 층: `[1,2,3,5,6,8,10]` — 4·7·9층을 거치지 않는다.
`PowerBand.MultiFloor`(요구의 170~219%) 이상이 추가 층을 준다(`PowerThresholds.cs:20`).

`CURRENT_PHASE.md` §2.2의 제약 "다층 상승이 **최종 층과 빌드 보상 층**을 건너뛰지 않음"은
**충족된다** — 2·5·8(빌드 보상)과 10(최종)이 모두 방문된다.

그러나 커리큘럼 관점에서는 4층(캐스케이드)·7층(증식체)·9층(과수확 강조)을
**한 번도 가르치지 못하는 런이 정상 경로**라는 뜻이다. 설계 의도인지 결함인지 판정 필요.

## 7. 분류 요약

### 완료 (증거 확인)

- 결정론적 3×3 룰렛 코어, 저항체 3종, 계약 2종, 개수 정화, 직선·직교 연결, 캐스케이드 20 캡
- 승객·부품 11종 (`BuildCatalog`) 및 실제 오브젝트 배치 (`BuildFigureView`)
- 총중량·허용중량·과적·요구 전력 증가
- 과수확 잠금·해제·추가 스핀
- 위험 4단계 로직 + `RiskStateView` 조명·험·진동
- 사고 기록기 (`AccidentRecorder`, `FloorRecord`)
- `PowerBand.Jettison` 대가 지불 (§4)
- 캡처 하네스 (`TenFloorCaptureRig`) 18장, 성능 하네스 (`HeroSlicePerfProbe`)
- EditMode 89건 전부 통과

### 부분 완료

- **PlayMode 증거가 현재 코드보다 오래됐다** (§2) — 재실행 필요
- **PlayMode 시드가 2개** (요구 3개) — 씬 경로 한정
- 커리큘럼 층 매핑이 목표 지시와 다르다 (§5)
- 4·7·9층이 정상 경로에서 건너뛰어진다 (§6)
- 승객 반응(위험 상태 동기화)이 `RiskStateView`에 없다 — 이전 감사 기록, **미확인**
- 사고 기록기가 `ScreenSpaceOverlay`라 고정 캡처 세트에 구조적으로 못 들어간다 — **미확인**

### 미완료

- 현재 캡처 세트에 대한 **독립 시각 평가** (§3)
- `P2-Gate H` 독립 재검토
- Windows 빌드 산출 또는 차단 원인 기록
- 최대 적재 + Critical 조건의 성능 측정 (`P2-Gate G`) — **미확인**

### 고장

- 없음. 컴파일·콘솔·EditMode 전부 정상.

### 환경 제약

| 항목 | 내용 |
|---|---|
| 기준 하드웨어 | `TECH_SPEC.md` §13의 Ryzen 7 5700 / RTX 3070 중 GPU만 일치. 성능 완료 선언 불가 |
| 캡처 베이스라인 | 이 기기(Windows/D3D12/RTX 3070) 기준으로만 유효 |
| 에디터 Play 측정 | 빌드 성능이 아니다. 게임 코드 비용과 에디터 비용을 분리해 기록한다 |
| asmdef 없음 | 스크립트 1개 수정에 전체 재컴파일 + 플레이 모드 종료 |

## 8. 진행 시작점

가장 높은 검증 완료 지점은 **EditMode 89 PASS + 컴파일 클린 + 캡처 18장 생성**이다.
여기서부터 이어간다. 재작성하지 않는다.

다음 단계는 §9(독립 감사 결과)가 채워진 뒤 확정한다.
