# TOPDOWN PROGRESS

> **각 로컬 커밋 전에 이 파일을 갱신한다.** 갱신하지 않은 커밋은 진행 기록이 없는 커밋이다.
> 서술은 `ProgressLog.md`가 맡는다. 이 파일은 **지금 어디에 있는가**만 짧게 적는다.

---

## 현재 위치

| 항목 | 값 |
|---|---|
| 현재 패스 | **Pass 1 — Breadth First Coverage** |
| 브랜치 | `agent/phase2-full-prototype` (main 대비 28+ 커밋 앞섬) |
| 마지막 정상 커밋 | `1992651` — 진행 문서 갱신 |
| 마지막 검증 통과 커밋 | **없음** — `verify-topdown.ps1`이 아직 통과한 적 없다 |
| 백로그 | `docs/TOPDOWN_MASTER_BACKLOG.md` |
| 갱신 시각 | 2026-08-01 (실증 감사 반영) |

## Required 상태 분포

`verify-topdown.ps1 -Stats` 출력과 일치해야 한다.

| 상태 | 개수 |
|---|---|
| `VERIFIED` | **64** |
| `CONNECTED` | 26 |
| `VISIBLE` | 0 |
| `SKELETON` | 16 |
| `NOT_STARTED` | 23 |
| **Required 합계** | **129** |

`UP-RUN-09`(돈)는 PRD §4.1에 없어 Deferred로 내렸다. **구현·테스트돼 있으므로 제거하지 않는다.**

---

## 다음 항목 — `NEXT_TOPDOWN_PLAN.md` Pass 1 순서

| 순 | ID | 항목 | 왜 이 순서인가 |
|---|---|---|---|
| 1 | `UP-TEST-05` | 텔레메트리 (스핀별 20필드) | **현재 가장 큰 범위 누락.** PRD §4.1 명시 항목이 통째로 없고, 없으면 이후 밸런스 판단이 전부 사후 추정이 된다 |
| 2 | `UP-NPC-02·03·05` | 승객 반응 이벤트 10종 · `PassengerReactionSet` · 동시 반응 제한 | PRD §4.1 #19가 10종 중 1종만 있다 |
| 3 | `UP-AUD-02·03·04·05` | 룰렛 사운드 10종 · 과수확 정적 · 비언어 음성 · 믹스 | 감각 채널 넷 중 소리만 비어 있다 (PRD §8.3, §11) |
| 4 | `UP-PLAT-04` | `TargetHardwareProfile` | **PRD §13.1이 미지정 상태에서 성능 완료 선언을 금지** — Pass 4 선행 조건 |
| 5 | `UP-POWER-07`·`UP-RISK-07`·`UP-RISK-08`·`UP-PLAT-05`·`UP-TECH-09` | 데이터 프로파일 | §14.3 권장 13종 중 에셋 실재 **0종** |
| 6 | `UP-REC-04·05` | 사고 기록기의 물리적 형태 | PRD §10.1은 기계식 프린터를 요구. 현재 오버레이 텍스트 |
| 7 | `UP-VIS-04·05` | 스타일 셰이더 · 파티클 | Pass 3 선행. Pass 1에서는 존재만 |
| 8 | `UP-TECH-06·07·08` | 풀링 · 렌더링 예산 · 메모리 누적 | Pass 4 증거. Pass 1에서는 측정 지점만 |
| 9 | `UP-TEST-08·09` | 5연쇄 영상 · Critical→과수확 영상 | PRD §17.6 증거 산출물. 하네스에 녹화 없음 |
| 10 | `UP-CORE-14`·`UP-DOC-01·02` | 방어 테스트 · Notion 문서 정합 | 최상위 문서가 코드와 어긋난 상태를 오래 두지 않는다 |

---

## 차단 사항

| 종류 | 항목 | 누가 풀 수 있는가 |
|---|---|---|
| **게이트 꺼짐** | `.claude/settings.local.json`의 `env`에 `SKIP_TOPDOWN_GATE=1`이 있다. **자율 개발을 시작하기 전에 지울 것** | 에이전트. 세션 시작 알림이 매번 상기시킨다 |
| 외부 차단 | 없음 | — |
| 승인 대기 | 14건 (`PENDING_DECISIONS.md`) | 사용자. **작업을 멈추지 않는다** — 프리셋으로 진행 |
| 반복 실패 | `UP-FIX-02` (임계점 눈금 숫자) 3회 실패 | 배치 결정이 필요하다. 같은 층위의 4번째 시도 금지 |
| 측정 결함 | `UP-TECH-04` — 중앙값이 vSync 상한(8.33ms)에 걸려 90 FPS 목표를 판정할 수 없다 | 프로브를 상한 없는 측정으로 바꿔야 한다 |
| 미커밋 오염 | 폰트 아틀라스가 **글리프 순손실** 상태로 방치돼 있다 (`NanumGothic SDF.asset`) | `git checkout HEAD -- <경로>`. 커밋하면 한글 렌더링이 깨진다 |

---

## 완료 항목

Required 129건 중 **64건이 코드·씬·테스트 증거를 모두 갖췄다.**
목록과 근거는 `NEXT_TOPDOWN_PLAN.md` §5 「재작성하면 안 되는 시스템」.

핵심: 결정론적 3×3 룰렛 · 심볼 3종 · 계약 2종 · 정화/직선/연결/캐스케이드 ·
두 레버 · 전력과 임계점 · 위험 4단계 판정 · 사고 기록 데이터 · 승객·부품·무게·과적 ·
10층 커리큘럼과 클램프 · Windows 빌드 · 캡처/성능/테스트 하네스.

**"판정"과 "연출"을 구분한다** — 위험 4단계 판정은 VERIFIED지만 Collapse 연출은
SKELETON이고, 사고 기록 데이터는 VERIFIED지만 물리적 형태는 SKELETON이다.

---

## 마일스톤 로그

형식: `YYYY-MM-DD · <커밋> · <패스> · <완료한 ID> · <테스트 결과>`

```
2026-08-01 · 27dab79 · Pass 1 · 탑다운 실행 구조 설치 (백로그 130 / 검증기 / Stop hook 2개) · 게임 코드 변경 없음, EditMode 91 PASS / PlayMode 394 PASS 유지
2026-08-01 · 1992651 · Pass 1 · 진행 문서에 마지막 정상 커밋 기록 · 변경 없음
2026-08-01 ·  (이번) · Pass 1 · 실증 감사와 130건 재분류 (VERIFIED 0 → 64) · 게임 코드·씬 무수정, 읽기 전용 검증만
```
</content>
