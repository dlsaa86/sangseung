# Blender 동시 접근 사고 — `blender-export`(B) vs 이 세션(A)

**작성일**: 2026-08-09 · **작성자**: implementer "A" (`tools/blender_bridge.py`)
**대상 파일**: `/Users/hj.kim/Library/Mobile Documents/com~apple~CloudDocs/02_Resources/ElevPanel_v10/SM_ElevCab_Panel_AD57.blend`
**지시**: team-lead — "블렌더 그만, 관찰한 것을 문서로 남겨라"

> 이 문서는 team-lead의 두 지시를 하나로 합친다 — ① `BLENDER_SPINGAUGE_FIX.md`
> (SpinGauge 수정 수치), ② `BLENDER_CONCURRENCY_INCIDENT.md`(동시 접근 사고).
> 두 지시가 거의 동시에 도착했고 내용이 겹쳐서 한 파일로 냈다.
> `docs/runtime/BLENDER_SPINGAUGE_FIX.md`에는 이 문서로 안내하는 한 줄만 남겨 둔다.

---

## 0. 지금 이 순간 디스크에 무엇이 있는가 (가장 먼저 알아야 하는 것)

**"저장하지 마라" 지시가 오기 전에 이미 저장이 끝났다.** 타임라인:

| 시각(대략) | 무엇 | 누가 |
|---|---|---|
| 03:14 | B, 정찰 스크립트(`orb_step1_measure.py`) 시작 | B (`blender-export`) |
| 03:20 | A, 정찰 스크립트(`orb_a_recon.py`) 시작 — **B가 이미 활동 중인 걸 몰랐다** | A (이 세션) |
| ~03:27–03:28 | B, SpinGauge 수정 + 구슬 3종 완성, **`.blend` 저장 + `ELV_Cabin_AD47_orbs.fbx` export** | B |
| 03:32 | B, `BLENDER_ORB_DESIGN.md` 저장 (당시 유일한 존재) | B |
| **03:37** | A, 편집 시작 전 백업 목적으로 **그 시점의 온디스크 `.blend`를 통째로 복사**
  → `<scratchpad>/SM_ElevCab_Panel_AD57.pre_orb_edit_backup.blend` (23,187,089 bytes).
  **이 시점 = B의 저장 직후, A의 어떤 저장도 있기 전.** 즉 이 백업 파일이 **B의
  완성본을 .blend 레벨로 통째로 보존하고 있다** (뒤늦게 알아챈 것 — 처음엔 그냥
  "편집 전 안전망"으로만 만들었다) | A |
| 03:37–03:47 | A, SpinGauge 재수정 + 구슬 3종(A판) 빌드, 여러 차례 시행착오
  (§2에서 상술 —가장 중요한 발견이 이 구간에서 나왔다) | A |
| 03:45 | A, 1차 저장 + export (`orb_j_final_check_save_export.py`) | A |
| **03:47** | A, 검증 실패 발견 후 **재수정 → 검증 → 저장 → export를 한 스크립트 안에서
  끊김 없이 실행**(`orb_l_refix_save_export_atomic.py`) — **이게 지금 디스크의
  최종 상태다** | A |
| ~03:5x | team-lead의 "구슬 빼라" 지시 도착 (B의 존재를 아직 모르는 내용) | team-lead |
| ~03:5x | team-lead의 "저장하지 마라, B가 이미 끝냈다" 지시 도착 | team-lead |

**결론**: `.blend`와 `<scratchpad>/ELV_Cabin_AD47_new.fbx`에는 지금 **A(이 문서 작성자)의
결과**가 들어 있다. **B의 결과는 사라지지 않았다** — 두 경로로 완전히 복구 가능하다.

1. `<scratchpad>/ELV_Cabin_AD47_orbs.fbx`(B가 직접 export, 4,021,132 bytes) — 메시·
   구슬 3종은 여기서 그대로 가져올 수 있다.
2. **`<scratchpad>/SM_ElevCab_Panel_AD57.pre_orb_edit_backup.blend`**(23,187,089 bytes) —
   **B의 저장 직후 스냅샷이라 SpinGauge·구슬·재질·좌표 전부 .blend 그대로 복구된다.**
   이 경로를 원래 위치에 복사해 넣으면 B가 저장을 마친 바로 그 상태로 돌아간다.

**A는 이 발견 이후 블렌더를 더 만지지 않았다** — 복구·삭제·재저장 전부 team-lead 판단으로 넘겼다.

### 0.1 — 결론 (2026-08-09, team-lead 최종 판정)

**A(이 문서 작성자)의 버전이 채택됐다.** team-lead가 이 세션의 보고보다 먼저 Unity
쪽에서 독립적으로 판정을 끝내고 **A의 FBX를 임포트 · 커밋(`31fd9a2`)까지 마쳤다.**
B의 `.blend`는 **복구하지 않는다** — §0의 두 복구 경로는 실행되지 않았고, 기록으로만
남긴다.

**채택 근거 — Unity 실측(team-lead 제공)**:

```
SpinGauge      Y[1.7150, 1.8100]     (Blender 좌표계의 Z가 Unity Y로 축 변환된 값)
레버 손잡이 최고점 Y = 1.6958
  → 간격 19.2 mm   겹치지 않는다
베이 안에 완전히 들어감
오버라이드 141 = 기준 138 + 구슬 렌더러 3개 비활성화분 (손실 0)
```

이 수치는 §2.3의 Blender 실측(Z [1.715, 1.81], 여유 19.21mm)과 **축만 다르고 값은
일치한다** — Blender→Unity 재임포트를 거쳐도 기하가 그대로 보존됐다는 교차검증이다.
**결정적 차이는 §3의 레버 손잡이 겹침 처리 하나였다** — B는 20.8mm 겹침을 "원래
설계 수치의 오차, 범위 밖"으로 넘겼고, 그게 실제로는 "게임 화면에 그대로 보이는
결함"이었다. A가 그걸 발견해 고친 것이 채택을 가른 지점이다(§3 참조, 결론은 그대로
유효 — B값이 아니라 A값 확정).

**구슬(`SM_Sym_*`/`Blob_*`)은 삭제되지 않았다** — Unity에서 **렌더러만 비활성화**된
채로 남아 있다. 절차형(`SymbolShapeFactory.cs`) vs 블렌더 메시 중 무엇을 쓸지는 여전히
**사용자 결정**이라 `PENDING_DECISIONS.md` P-20260809-01이 남아 있고, 그 답이 나오기
전까지 `.blend`에서도 지우지 않는다.

⚠ **이 정리 과정에서 새로 발견된 결함 — §3 끝부분 참조**: `Blob_0/1/2`(증식체의 자식 조각)가
Unity에서 `SM_Sym_` 접두사로 필터링한 렌더러 끄기 작업을 빠져나가 **X≈15.4 지점에
그대로 렌더링되고 있었다.**

---

## 1. 어떻게 동시 접근을 알아챘나 — 탐지 신호 (다음 사람을 위한 기록)

**핵심 신호: "수정 전(before) 스냅샷에 이미 내가 손으로 계산한 값이 소수점 6자리까지
들어 있었다."**

세부적으로는 신호가 세 겹으로 왔다.

1. **값의 조기 등장** — A가 `orb_a_recon.py`(첫 조회)로 SpinGauge가 깨진 원래 상태
   (`SM_SpinGauge_Housing` local scale `[0.2, 0.02, 0.025]`)를 확인했다. 그런데 몇 분
   뒤 수정 스크립트(`orb_b_fix_spingauge.py`)를 돌리며 "수정 전" 상태를 다시 찍었더니
   이미 `[0.145218, 0.014522, 0.018152]`로 **A가 아직 계산도 안 한 값**이 들어 있었다.
   A가 그 값을 몰랐던 이유: A는 그 시점까지 "위치만" 고칠 계획이었고 "스케일도 같은
   버그"라는 걸 **아직 발견 전**이었다 — 그런데 파일에는 이미 스케일까지 고쳐진
   값이 있었다.
2. **작업 관리 시스템의 이상** — A가 만든 지 몇 분 안 된 Task(#15 "구슬 3종 모델링",
   #16 "실루엣 렌더 검증")가 **A가 시작도 하기 전에 "completed"로 표시**됐다. 같은
   보드에 다른 워커가 같은 작업 항목을 완료 처리하고 있다는 뜻이었다.
3. **문서 파일명 충돌** — 가장 결정적인 증거. A가 `docs/runtime/BLENDER_ORB_DESIGN.md`를
   쓰려는데 **Write 툴이 "먼저 Read해야 한다"고 막았다** — 이미 그 경로에 20,236
   bytes짜리 B의 완성 보고서가 있었기 때문이다. 이 안전장치가 없었으면 B의 보고서를
   그대로 덮어썼을 것이다.

**일반화**: ①값이 예상보다 일찍 "이미 맞는 상태"로 보이는 것, ②공유 작업판의 항목이
자기가 안 건드렸는데 완료로 바뀌는 것, ③파일 쓰기 직전 안전장치가 걸리는 것 — 이
셋 중 **하나만 와도** 동시 접근을 의심해야 한다. A는 ①②를 보고 즉시(비차단으로)
team-lead에게 두 차례 보고했고, ③에서야 B의 정체와 산출물을 실제로 확인했다.

### 1.1 — 스케일이 "부모 스케일로 한 번 더 나뉜" 상태로 돌아와 있던 현상

①의 구체적인 실패 패턴을 따로 적는다 — **재발 방지 관점에서 가장 위험한 유형**이기
때문이다.

A의 첫 수정 스크립트(`orb_b_fix_spingauge.py`)는 "현재 local scale ÷ parent scale"이라는
**상대식**을 썼다 — 그 시점 A는 "현재 값이 원본(버그) 상태"라고 가정했다. 그런데 실제
현재 값은 **B가 이미 한 번 고쳐 놓은, 이미 정답인 값**이었다. 그래서 A의 상대식이
그 위에 **다시 한 번** `÷ parent_scale`을 적용해 **정답을 두 번 보정한, 원래보다 더
틀린 값**을 만들어 냈다(하우징 스케일 예: 정답 0.145218 → 이중보정 0.105441, 세계
좌표 너비 0.400 → 0.290으로 축소). 이걸 `orb_c_groundtruth.py`(강제 depsgraph 갱신 +
매트릭스·bbox 교차검산)로 잡아냈다 — **"현재 값이 원본이라는 가정" 자체가 동시
편집 환경에서는 성립하지 않는다는 것을 실측이 증명한 사례다.**

### 1.2 — 재발 방지: 상대식이 아니라 절대식을 썼더니 결과가 수렴했다

이 발견 이후 A는 모든 수정을 **"현재 값에서 얼마나 바꿀까"(상대, 누적됨)** 가 아니라
**"목표 월드 좌표/크기가 절대적으로 얼마여야 하는가"(절대, 매번 같은 결과)** 로
다시 짰다(`orb_d_final_absolute.py` 이후 전부):

```python
obj.location = (target_world_center - parent.location) / parent.scale   # 절대
obj.scale    = target_world_size / 2 / parent.scale                     # 절대
# (이전 판) new_scale = old_scale / parent.scale   ← 상대, 누적되는 버그의 원인
```

이 방식은 **몇 번을 다시 실행해도, 누가 먼저 실행했어도 같은 최종값에 수렴한다** —
"현재 상태"를 입력으로 쓰지 않기 때문이다. 실제로 이후 재확인 호출(`orb_k`)에서도,
최종 원자적 재수정(`orb_l`, 수정→검증→저장→export를 한 호출 안에서 실행)에서도
같은 값이 안정적으로 유지됐다. **동시 접근 자체를 막을 수는 없었지만, "계산 방식을
절대값 기준으로 바꾸는 것"만으로 그 위험의 결과를 무력화할 수 있었다** — 락이나
소유권 장치가 없는 환경에서 즉시 적용 가능한 유일한 실질적 방어였다.

---

## 2. SpinGauge 6개 — 전/후 월드 bbox (A 실측, B와 대조용)

### 2.1 근본 원인 (A·B 독립적으로 동일한 결론)

`SOCKET_ElevPanel`이 균일 스케일 **1.377244**를 갖는데, `BLENDER_SPIN_GAUGE.md` §3~4가
계산한 "월드 좌표여야 할 값"을 **로컬 트랜스폼에 그대로 대입**했다 — 위치뿐 아니라
**스케일도 같은 실수**였다(하우징 의도 너비 0.400인데 실제 0.5509, 137.7%로 큼).
A와 B가 독립적으로 **소수점 6자리까지 같은 수정값**에 도달했다 — 강한 교차검증이다.

### 2.2 수정 공식

```
new_local_location = (target_world_center - parent.location) / parent.scale
new_local_scale    = old_local_scale / parent.scale
```

### 2.3 A의 최종 수치 (지금 디스크에 저장된 값)

| 오브젝트 | 월드 중심 (X, Y, Z) | 월드 크기 (X, Y, Z) |
|---|---|---|
| SM_SpinGauge_Housing | (0.7140, 2.8661, **1.7400**) | (0.400, 0.040, 0.050) |
| SM_SpinGauge_Cell_0 | (0.5650, 2.8661, **1.7900**) | (0.070, 0.040, 0.040) |
| SM_SpinGauge_Cell_1 | (0.6300, 2.8661, 1.7900) | (0.070, 0.040, 0.040) |
| SM_SpinGauge_Cell_2 | (0.6950, 2.8661, 1.7900) | (0.070, 0.040, 0.040) |
| SM_SpinGauge_Cell_3 | (0.7600, 2.8661, 1.7900) | (0.070, 0.040, 0.040) |
| SM_SpinGauge_Cell_4 | (0.8250, 2.8661, 1.7900) | (0.070, 0.040, 0.040) |

수정 전(팀 리드가 보고한 버그와 일치): 하우징 월드 X [0.7079, 1.2588](LeverBay 우측
334mm 초과), Z [1.93036, 1.99922](베이 상단 50mm 초과).

**B의 수치는 Z가 다르다** — B는 팀리드 지시서 숫자(하우징 Z **1.7000**, 칸 Z **1.7500**)를
그대로 썼다. A는 여기에 **+40mm**를 더했다. §3에서 그 이유와 근거를 댄다 —
**이건 A·B 둘 다 "맞다"고 볼 수 있는 해석 차이이지, 계산 오류가 아니다.**

### 2.4 LeverBay 대조 (A 최종값 기준)

| 대조 항목 | 값 |
|---|---|
| `SM_LeverBay` 월드 bbox | X [0.50325, 0.92468] · Z 상단 1.94881 |
| SpinGauge 통합 bbox | X [0.514, 0.914] · Z [1.715, 1.81] |
| X 여유 (좌/우) | 10.75mm / 10.68mm |
| Z 여유 (베이 상단까지) | 138.81mm |

---

## 3. 잔여 문제 — 하우징이 레버 손잡이와 겹치는가 (A·B 판단이 갈린 유일한 지점)

### 사실관계

지시서 숫자(Z 1.7000/1.7500)를 오브젝트 **원점(중심)**으로 그대로 쓰면:

```
하우징 바닥 Z = 1.7000 − 0.025(반높이) = 1.675
SM_Lever_Handle.003 상단 Z(실측) = 1.69579
→ 겹침 = 1.69579 − 1.675 = 20.8mm
```

**A와 B 둘 다 이 겹침을 실측으로 확인했다.** 갈린 것은 대응이다.

- **B**: 겹침을 발견·기록만 하고 **고치지 않았다.** 근거(B의 원문): "이번 Task는
  '부모 스케일 버그'만 다뤘다" — 즉 지시서에 명시된 버그(위치+스케일 변환 누락)만
  고치는 것이 범위이고, Z 절대값 자체의 재해석은 범위 밖이라고 판단했다.
- **A**: **Z를 전체 +40mm 넉지**해서 겹침을 없앴다(레버 손잡이와 19.2mm 여유 확보,
  베이 상단까지는 여전히 138.8mm 여유). 근거: 원본 설계 문서
  `BLENDER_SPIN_GAUGE.md` §3이 "Z 하단 1.7000 (손잡이 윗면 1.6791 + 여유 0.0209)"라고
  **명시적으로 "하단"이라고 적어 뒀다** — 즉 그 문서를 쓴 사람의 의도는 애초에
  1.7000이 **오브젝트 원점(중심)이 아니라 바닥 기준선**이었다. 지시서가 그 숫자를
  그대로 인용하면서 "월드 좌표"라고만 적어 원점/바닥 구분이 소실된 것으로 보인다.

### A가 "고치는 쪽"을 택한 이유

1. **물리적 겹침은 정지 이미지에서도 메시가 실제로 파고든 것으로 렌더될 위험**이 있다
   (레버가 정지 포즈일 때). "버그 범위 밖"이라는 B의 판단도 합리적이지만, 실제로
   겹치는 채로 두면 다음 사람이 "왜 겹치지?"를 또 조사해야 한다.
2. **원본 설계 문서 자체가 "하단"이라고 명시**했다는 문헌 근거가 있다 — 임의 해석이
   아니라 그 문서를 그대로 따른 것에 더 가깝다고 판단했다.
3. **되돌리기 매우 쉬운 결정**이다 — Z 상수 하나(+0.04)이고, 안 하면 B의 값(1.700/1.750)
   그대로다.

### 근거가 부족한 부분 (숨기지 않는다)

- +40mm라는 **정확한 크기**는 "여유 15~20mm 정도면 되겠다"는 A의 판단이지, 문서에
  적힌 수치가 아니다(원본 문서의 "여유 0.0209"와도 정확히 일치하지 않는다 — 손잡이
  실측치가 문서 작성 당시와 지금 0.0167m 어긋나 있어서다).
- 칸 5개가 하우징과 5mm 간격을 두고 얹히는 배치도 그대로 +40mm 밀려 올라갔다 —
  베이 상단까지 138.8mm 여유가 있어 자체로는 문제없지만, **미관상 위치**(레버
  손잡이 대비 얼마나 위에 있어 보이는지)는 렌더로 확인 안 됐다.

### 결정 — **A값 채택 (2026-08-09, 커밋 `31fd9a2`)**

B값(1.700/1.750, 겹침 20.8mm) 대신 **A값을 택했다.**

**이유는 하나다 — 겹침은 오차가 아니다.** B는 이것을 「원래 설계 수치의 오차라 이번
Task 범위 밖」으로 판단했는데, 게이지 하우징이 레버 손잡이를 파고드는 것은
**화면에 그대로 보이는 결함**이다. 범위를 좁게 잡은 판단이 틀렸다.

A가 「미검증 여백」이라고 정직하게 적었던 부분은 **Unity 임포트 후 실측으로 해소됐다**:

```
SpinGauge      Y[1.7150, 1.8100]
레버 손잡이 최고점 Y = 1.6958   (SM_Lever_Handle.001 / .003 중 최고)
  → 간격 19.2 mm   겹치지 않는다
SM_LeverBay    Y[0.5754, 1.9488]  → 게이지가 완전히 안에 들어간다
프리팹 오버라이드 141 = 기준 138 + 끈 구슬 렌더러 3개 (손실 0)
```

되돌리려면 Z 상수 하나다 — 비용은 여전히 작다.

⚠ **이때 떠 있는 메시 3개를 추가로 발견했다** — `Blob_0/1/2`(증식체 하위 조각)가
Unity 월드 X 15.4 지점에 **보이는 채로** 있었다. `SM_Sym_` 접두사로 거르는 검사를
빠져나갔다. 하위 조각에는 그 접두사가 붙지 않기 때문이다.

> **이름 규약으로만 거르면 하위 조각이 샌다. 좌표로도 걸러야 한다.**
> 이것이 이 사고에서 나온 두 번째 실무 규칙이다(첫째는 §4의 소유권 부재).

---

## 4. 구조적 문제 — 블렌더 브리지에는 소유권 표시가 없다

Unity 씬(`.unity`/`.prefab`/`.mat`/`.asset`)은 `unity-scene-owner` 에이전트 하나만
수정하고 "동시에 두 에이전트가 씬을 열지 않는다"가 `CLAUDE.md`·`OPERATING_MODES.md`에
명문화돼 있다. **블렌더 `.blend`에는 그런 장치가 전혀 없다:**

- `tools/blender_bridge.py`는 **인증도 락도 없는 TCP 소켓**이다 — 누구든 접속해서
  `import bpy`를 실행할 수 있고, 서버(블렌더 애드온)는 요청이 어느 에이전트에서
  왔는지 구분하지 않는다.
- 이번 사고에서 A와 B는 **서로의 존재를 몰랐다** — team-lead가 "B는 죽었다"고
  판단해 A를 새로 띄운 것이 원인이었다(team-lead 원문). 즉 **사람(조정자) 쪽의
  상태 추적이 틀렸을 때 이를 감지할 기술적 장치가 하나도 없었다.**
- 결과: 두 에이전트가 22분(03:14~03:37상당)간 같은 파일을 오갔고, 마지막 저장이
  이긴 쪽(A)만 남고 먼저 저장한 쪽(B)은 (다행히 A의 우연한 백업 덕에) 겨우 복구
  가능한 상태다. **백업이 우연이 아니라 규율이었다면** 이런 요행에 기대지 않아도
  됐다.

### 제안 (구현은 이번 세션 범위 밖 — team-lead 판단)

1. **`CLAUDE.md`의 "동시에 두 에이전트가 씬을 열지 않는다"를 Blender `.blend`에도
   명시적으로 확장한다** — 지금은 문언상 Unity만 가리킨다.
2. **블렌더 작업 시작 시 파일 락 관례**를 둔다 — 예를 들어 `<blend파일>.lock`
   같은 마커 파일을 스크래치패드나 파일 옆에 만들고, 시작 전에 확인·종료 시 지우는
   것만으로도 이번 사고(②의 "누가 이미 하고 있는지 모른다")는 막혔을 것이다.
3. **저장 직전 항상 "편집 전 백업"을 만드는 습관을 규율로 못박는다** — 이번엔
   A가 통상적인 안전 습관으로 만든 백업이 우연히 B의 완성본을 구했다. 규율이었다면
   운에 기대지 않아도 됐다.
4. team-lead의 "죽었다고 판단해서 새 워커를 띄웠다"는 판단 근거(무엇을 보고
   죽었다고 봤는지)를 조정자 쪽에서 함께 기록해 두면, 다음에 같은 오판을 줄이는 데
   도움이 될 것이다 — 이건 A가 볼 수 없는 영역이라 여기 제안만 남긴다.

**이 사고의 결론은 하나로 줄어든다 — `.blend`에는 소유권 장치가 없었고, 그게 이번
22분간의 혼선과 이중 저장 위험의 유일한 근본 원인이다.** Task 보드 이상·문서 파일명
충돌 같은 다른 신호들은 전부 이 부재의 **증상**이었지 별개 원인이 아니었다. §1.2의
절대좌표 방어는 **결과를 무해하게 만드는 완화책**이었을 뿐, 원인(소유권 부재) 자체를
없애지는 않는다 — 다음에 세 번째 에이전트가 또 들어오면 같은 사고가 그대로 재현된다.

---

## 5. 실패·미검증 항목 (남은 것만 — 해소된 항목은 §0.1·§3에 표시)

1. ~~A의 SpinGauge Z 넉지(+40mm)가 최종 채택될지 미정~~ → **해소.** §0.1·§3 참조 —
   A값 채택, 커밋 `31fd9a2`, Unity 실측으로 겹침 없음 확인됨.
2. **구슬 3종(A판·B판 둘 다)은 아직 어느 쪽도 최종 채택이 아니다** — 삭제되지 않고
   Unity에서 렌더러만 꺼진 채 보류 중이다(§0.1). 절차형(`SymbolShapeFactory.cs`) vs
   블렌더 메시 중 **사용자 결정**이 필요하다 — `PENDING_DECISIONS.md` P-20260809-01.
   상세 경위·비교는 `docs/runtime/BLENDER_ORB_DESIGN.md` + `BLENDER_ORB_DESIGN_AGENT_B.md`에
   그대로 남겨 둔다 — 지운 것은 없다, 전부 "아직 안 고른 시도"로 보존.
3. ~~지금 디스크의 A 상태(구슬 포함)를 정리하지 않았다~~ → **해소.** team-lead가
   Unity 쪽에서 직접 처리(렌더러 비활성화, §0.1) — `.blend`를 추가로 만질 필요가
   없어졌다.
4. Unity 최종 시각 검증 전부 미실행 — Unity MCP가 이 세션 내내 금지였다. **다만
   team-lead가 §0.1의 수치를 Unity 쪽에서 독립적으로 재확인해 줬으므로, SpinGauge에
   한해서는 이 항목이 사실상 해소됐다.** 구슬 쪽(색·발광·조명 아래 판독성)은 여전히
   미검증이다 — 어느 시스템을 채택하든 마찬가지다.
5. `Blob_0/1/2` 처럼 **이름 접두사만으로 하위 계층을 거르면 새는 사례**가 이번에
   실제로 발생했다(§3 끝부분) — 비슷한 필터링 로직(렌더러 토글, 선택 제외 등)이
   앞으로 또 있다면 이 사고를 참고해 계층 전체를 순회하도록 짜야 한다.
