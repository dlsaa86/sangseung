# Progress Log — agent/phase1-hero-slice

> 시작: 2026-07-30 / 기준 커밋 `c0dac27`
> 계획: `ImplementationPlan.md` · 감사: `GapAnalysis.md`

시간순 기록. **실패와 미완료를 지우지 않는다.**

---

## 2026-07-30 — 세션 시작

### 환경 확인
- Unity 6000.5.5f1 / URP 17.5.0 / Metal / **Apple M5 / macOS 26.5.2 / 24GB**
- 에디터가 **이름 없는 빈 씬에서 Play 모드로 방치**되어 있었다 (`KNOWN_ISSUES` A-2 재발).
  → Play 종료 후 `Prototype_Elevator.unity` 오픈.
- `runInBackground = True` — `KNOWN_ISSUES` A-3의 포커스 문제는 이번 세션에 해당 없음.
- 작업 트리 비청결: `Packages/manifest.json`, `packages-lock.json`
  (`com.unity.ai.assistant` 2.16.0-pre.1 → 2.17.0-pre.1, 에디터가 자체 수행).
  되돌리지 않고 브랜치에 가져감 → 승인 필요 항목.

### 기준선
- `Ascend/Run Spin Tests` → **10 PASS / 0 FAIL**
- 컴파일 오류 0

### 산출
- `docs/runtime/GapAnalysis.md` — 갭 14건 (BLOCKER 4, HIGH 3, MEDIUM 4, LOW 3)
- `docs/runtime/ImplementationPlan.md` — WP-A ~ WP-I

### 판단
`SpinEngine` 재작성 근거 없음. 이 저장소는 코어가 없는 게 아니라 **코어가 화면과
연결되지 않은** 상태다. 세션 무게중심을 신규 판정 로직이 아니라 연결·연출·상태 표현에 둔다.

---

## WP-A — Gate A 코어 정합  ✅  (커밋 `a23a7d3`)

- 캐스케이드 하드 캡 8 → **20** (`MASTER_PRD.md` §6 위반이었다).
- `SpinResolution.CascadeCapReached` + `SpinEngine.CascadeCapReached` 이벤트 + `DescribeCascade()`.
  캡 도달이 자연 종료와 구분되지 않으면 **무한 루프 방지가 동작했는지 증명할 수 없다.**
- 시드 파생을 `SpinSeed.Derive(runSeed, floor, spinIndex)` 좌표 방식으로 교체.
  기존 순차 스트림은 호출 순서에 의존해 "3층 2번째 스핀"의 단독 재현이 불가능했다.
- `SpinResolution.ToLogLine()` — 로그 한 줄로 스핀 재현 (`TECH_SPEC.md` §11).
- 테스트 12종 추가. **33 PASS / 0 FAIL**

## WP-B — Hero Slice 층 계획  ✅  (커밋 `eaf9121`)

`CURRENT_PHASE.md`와 10층 커리큘럼의 충돌을 `IFloorPlanSource` 분리로 해소
(→ `DECISION_LOG` D-20260730-07). 10층 커리큘럼 보존을 테스트로 고정.

### 요구 전력 측정 (400시드 × 계약 3종, 헤드리스)

누적 전력 p10 / 중앙 / p90:

| 계약 | 1스핀 | 2스핀 | 3스핀 | 4스핀 | 5스핀 |
|---|---|---|---|---|---|
| 계약 없음 | 42 / 66 / 394 | 100 / 247 / 712 | 170 / 388 / 1129 | 252 / 583 / 1341 | 389 / 767 / 1609 |
| 흡수체 계약 | 50 / 146 / 858 | 159 / 507 / 1763 | 303 / 931 / 2440 | 558 / 1298 / 3125 | 786 / 1716 / 3767 |
| 증식체 계약 | 68 / 122 / 795 | 168 / 464 / 1804 | 320 / 831 / 2421 | 564 / 1194 / 2848 | 716 / 1591 / 3592 |

요구 전력 후보별 (무계약 기준):

| 요구 전력 | 달성 스핀 중앙 | 5스핀 내 달성률 | 3스핀 이하 달성률 |
|---:|---:|---:|---:|
| 300 | 3 | 96% | 70% |
| 360 | 3 | 91% | 55% |
| 400 | 3 | 89% | 49% |
| **460** | **3** | **80%** | **47%** |
| 520 | 3 | 72% | 39% |
| 600 | 4 | 67% | 29% |

**460 채택** — 달성 중앙 3스핀이면 2스핀이 남아 과수확이 실제 선택이 되고, 20%는 실패해 판돈이 산다.

### 밸런스 관찰 — 미해결

**계약 없음이 열등 선택지다.** 5스핀 중앙 전력이 무계약 767 vs 흡수체 1716 / 증식체 1591로
2배 이상 벌어진다. 잔류 대가 배수(×1.8~2.0)가 출현률·보상 증가를 상쇄하지 못한다.
`MASTER_PRD.md` 검증 가설 4번("계약 선택이 의미 있는가")이 현재 구성에서는 성립하지 않는다.
흡수체 vs 증식체 사이의 선택(1716 vs 1591)은 대등하므로 Hero Slice의 검증 목적은 유지된다.
**최종 밸런스는 이번 Phase 제외 항목이므로 조정하지 않고 기록만 한다.**

## WP-C·E — 레버 분리 + 캐스케이드 연출  ✅  (커밋 `766c8ab`)

- 과수확 레버 신설(보호 덮개·잠금등·경고 띠·명판). 요구 전력 100% 전에는 손잡이 콜라이더가
  꺼져 조준 자체가 걸리지 않는다. 하우징은 조준 가능 — "왜 못 쓰는지"가 프롬프트로 읽혀야 한다.
- 실행 레버에서 추가 스핀 제거 (→ `DECISION_LOG` D-20260730-08).
- `SpinPresenter` — 확정된 `SpinResolution`을 통관 순차 공개 → 정화 맥동 → **빈칸** → 재충전
  순서로 재생. 패턴 등급별 맥동 횟수(개수 1 / 직선 2 / 연결 3 / 잭팟 4)로 원인을 구분한다.
- HUD의 키보드 게임플레이 경로 제거. 물체 쪽이 망가져도 키보드로 통과하던 이중 경로였다.
- PlayMode 하네스 신설 — `IInteractable.Interact()`를 부른다(= 클릭과 같은 경로).
  **26 PASS / 0 FAIL**

## WP-D·F — 계기판 + 위험 상태  ✅  (커밋 `5a920af`)

- `RiskEvaluator` (순수 C#, 히스테리시스), `RiskProfile` 3종 프리셋, `RiskStateView`가
  조명·경고등·물체 진동·절차생성 험 **네 채널**을 동시에 민다.
- `InstrumentPanelView` — `ElevatorGrayboxView`가 폐기된 `RunController`를 참조해 죽어 있던
  자리를 대신한다. 전력 게이지에 100/130/170/220% 눈금.

### PlayMode 검증이 잡아낸 결함 — 과수확 가중치

과수확 가중치 2.6에서 **"과수확 1회 후에도 Stable"** 로 실패했다.
`MASTER_PRD.md` §7이 과수확을 "공간적 사건"으로 규정하므로 이건 튜닝이 아니라 명세 위반이다.
Warning 진입선 위(3.2)로 올리고 `OverharvestWeight >= WarningEnter` 불변식을 테스트로 고정.

## WP-G — 사고 기록기  ✅  (커밋 `cc32376`)

`RunSession`은 확정 즉시 `Current`를 교체하므로 확정 뒤에 물어보면 이미 없다.
보고 있는 층을 직접 붙들고 있다가 `Result`가 채워지는 프레임에 기록한다.
층 중간의 **최고** 위험 단계를 누적한다 — 끝난 시점의 위험만 남기면 실패 원인이 사라진다.
**PlayMode 34 PASS / 0 FAIL**

## WP-I — 캡처 · 성능  ✅  (커밋 `947588b`)

### 고정 캡처가 잡아낸 결함 3건

1. **하이라이트가 심볼을 6배로 부풀렸다.** `child.localScale = Vector3.one * k`가 씬에서
   저작된 스케일(구 0.17 / 정육면체 0.16 / 캡슐 0.15)을 덮어썼다. 결과판 전체를 가리는
   흰 판때기가 캡처에 찍혔다. → 저작 스케일을 캐시해 **곱한다**.
2. **과수확 명판이 벽 하나를 덮고 좌우가 뒤집혔다.** fontSize를 월드 단위로 계산하고
   Y 180°를 걸었다. → 기존 계기판 라벨과 같은 규약(스케일 0.05 / fontSize 10, 회전 없음).
3. **계기판 3줄 상태 텍스트가 전력 게이지를 덮었고 "위험 위험"으로 읽혔다.**
   → 2줄 상한 + "위험도" 라벨 + 라벨 세로 간격 재배치.

캡처 없이 자동 테스트만 돌렸으면 셋 다 통과했을 결함이다.

### 고정 시드 3개

| 시드 | 용도 | 확인된 결과 |
|---|---|---|
| **1337** | 기준 진행 | 흡수체 계약, 3스핀에 511/460 달성, 과수확 1회 → 558.42 (정상 상승) |
| **7** | Critical 도달 | 증식체 계약, 과수확 2회 → 위험 점수 8.8, 전력 4008/460 |
| **12** | 깊은 연쇄 | 증식체 계약, 연쇄 6단계 (캡처는 4단계 시점) |

### 캡처 세트 (`Captures/HeroSlice/`, 1920×1080 / FOV 60 / 전용 카메라 RT)

`01_interior` `02_board_front` `03_contract_select` `04_stable` `05_critical`
`06_overharvest_access` `06b_overharvest_locked_compare` `07_cascade_deep` + `manifest.txt`

**Stable(04)과 Critical(05)은 동일 좌표·FOV·해상도**다 — 무음 비교의 전제.

### 성능 (참고치, 성능 완료 선언 아님)

기기: Apple M5 / Metal / macOS 26.5.2. 에디터 Play 모드, 게임뷰 979×485.
`TECH_SPEC.md` §13 기준 PC(Ryzen 7 5700 / RTX 3070 / Windows)가 아니므로
§13에 따라 **성능 완료를 선언하지 않는다.**

| 항목 | 값 |
|---|---|
| 판정 순수 비용 | 스핀당 **7.7 µs** / **336 B** (1000스핀, 연출·렌더 없이) |
| 유휴 프레임타임 | 중앙 0.84 ms / 95% 1.08 ms / 최악 1.62 ms |
| 스핀·캐스케이드 중 | 중앙 1.22 ms / 95% 4.58 ms / **최악 200.57 ms** |
| 유휴 GC Alloc | 프레임당 평균 **143 KB** |
| 유휴 GC Alloc — IMGUI HUD 끔 | 프레임당 평균 **15.6 KB** |

**§13의 "프레임당 0 B GC Alloc 목표"를 충족하지 못한다.**
유휴 143 KB 중 **127 KB(약 89%)가 IMGUI 디버그 HUD**다. 남은 15.6 KB는 뷰의 매 프레임
문자열 생성으로 추정된다. 판정 자체는 무관하다.
**최악 프레임 200 ms 스파이크는 원인 미확정** — GC 수집으로 추정되나 확인하지 않았다.
