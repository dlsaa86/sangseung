# Progress Log — agent/phase1-hero-slice

> 시작: 2026-07-30 / 기준 커밋 `c0dac27`
> 계획: `ImplementationPlan.md` · 감사: `GapAnalysis.md`

시간순 기록. **실패와 미완료를 지우지 않는다.**

---

## 2026-07-30 — 세션 시작

### 환경 확인
- Unity 6000.5.5f1 / URP 17.5.0 / Metal / **Apple M5 / macOS 26.5.2 / 24GB**
- 에디터가 **이름 없는 빈 씬에서 Play 모드로 방치**되어 있었다 (`KNOWN_ISSUES` A-2 재발).
  → Play 종료 후 `Prototype_Elevator.unity` 오픈.
- `runInBackground = True` — `KNOWN_ISSUES` A-3의 포커스 문제는 이번 세션에 해당 없음.
- 작업 트리 비청결: `Packages/manifest.json`, `packages-lock.json`
  (`com.unity.ai.assistant` 2.16.0-pre.1 → 2.17.0-pre.1, 에디터가 자체 수행).
  되돌리지 않고 브랜치에 가져감 → 승인 필요 항목.

### 기준선
- `Ascend/Run Spin Tests` → **10 PASS / 0 FAIL**
- 컴파일 오류 0

### 산출
- `docs/runtime/GapAnalysis.md` — 갭 14건 (BLOCKER 4, HIGH 3, MEDIUM 4, LOW 3)
- `docs/runtime/ImplementationPlan.md` — WP-A ~ WP-I

### 판단
`SpinEngine` 재작성 근거 없음. 이 저장소는 코어가 없는 게 아니라 **코어가 화면과
연결되지 않은** 상태다. 세션 무게중심을 신규 판정 로직이 아니라 연결·연출·상태 표현에 둔다.

---

## WP-A — Gate A 코어 정합  ✅  (커밋 `a23a7d3`)

- 캐스케이드 하드 캡 8 → **20** (`MASTER_PRD.md` §6 위반이었다).
- `SpinResolution.CascadeCapReached` + `SpinEngine.CascadeCapReached` 이벤트 + `DescribeCascade()`.
  캡 도달이 자연 종료와 구분되지 않으면 **무한 루프 방지가 동작했는지 증명할 수 없다.**
- 시드 파생을 `SpinSeed.Derive(runSeed, floor, spinIndex)` 좌표 방식으로 교체.
  기존 순차 스트림은 호출 순서에 의존해 "3층 2번째 스핀"의 단독 재현이 불가능했다.
- `SpinResolution.ToLogLine()` — 로그 한 줄로 스핀 재현 (`TECH_SPEC.md` §11).
- 테스트 12종 추가. **33 PASS / 0 FAIL**

## WP-B — Hero Slice 층 계획  ✅  (커밋 `eaf9121`)

`CURRENT_PHASE.md`와 10층 커리큘럼의 충돌을 `IFloorPlanSource` 분리로 해소
(→ `DECISION_LOG` D-20260730-07). 10층 커리큘럼 보존을 테스트로 고정.

### 요구 전력 측정 (400시드 × 계약 3종, 헤드리스)

누적 전력 p10 / 중앙 / p90:

| 계약 | 1스핀 | 2스핀 | 3스핀 | 4스핀 | 5스핀 |
|---|---|---|---|---|---|
| 계약 없음 | 42 / 66 / 394 | 100 / 247 / 712 | 170 / 388 / 1129 | 252 / 583 / 1341 | 389 / 767 / 1609 |
| 흡수체 계약 | 50 / 146 / 858 | 159 / 507 / 1763 | 303 / 931 / 2440 | 558 / 1298 / 3125 | 786 / 1716 / 3767 |
| 증식체 계약 | 68 / 122 / 795 | 168 / 464 / 1804 | 320 / 831 / 2421 | 564 / 1194 / 2848 | 716 / 1591 / 3592 |

요구 전력 후보별 (무계약 기준):

| 요구 전력 | 달성 스핀 중앙 | 5스핀 내 달성률 | 3스핀 이하 달성률 |
|---:|---:|---:|---:|
| 300 | 3 | 96% | 70% |
| 360 | 3 | 91% | 55% |
| 400 | 3 | 89% | 49% |
| **460** | **3** | **80%** | **47%** |
| 520 | 3 | 72% | 39% |
| 600 | 4 | 67% | 29% |

**460 채택** — 달성 중앙 3스핀이면 2스핀이 남아 과수확이 실제 선택이 되고, 20%는 실패해 판돈이 산다.

### 밸런스 관찰 — 미해결

**계약 없음이 열등 선택지다.** 5스핀 중앙 전력이 무계약 767 vs 흡수체 1716 / 증식체 1591로
2배 이상 벌어진다. 잔류 대가 배수(×1.8~2.0)가 출현률·보상 증가를 상쇄하지 못한다.
`MASTER_PRD.md` 검증 가설 4번("계약 선택이 의미 있는가")이 현재 구성에서는 성립하지 않는다.
흡수체 vs 증식체 사이의 선택(1716 vs 1591)은 대등하므로 Hero Slice의 검증 목적은 유지된다.
**최종 밸런스는 이번 Phase 제외 항목이므로 조정하지 않고 기록만 한다.**

## WP-C·E — 레버 분리 + 캐스케이드 연출  ✅  (커밋 `766c8ab`)

- 과수확 레버 신설(보호 덮개·잠금등·경고 띠·명판). 요구 전력 100% 전에는 손잡이 콜라이더가
  꺼져 조준 자체가 걸리지 않는다. 하우징은 조준 가능 — "왜 못 쓰는지"가 프롬프트로 읽혀야 한다.
- 실행 레버에서 추가 스핀 제거 (→ `DECISION_LOG` D-20260730-08).
- `SpinPresenter` — 확정된 `SpinResolution`을 통관 순차 공개 → 정화 맥동 → **빈칸** → 재충전
  순서로 재생. 패턴 등급별 맥동 횟수(개수 1 / 직선 2 / 연결 3 / 잭팟 4)로 원인을 구분한다.
- HUD의 키보드 게임플레이 경로 제거. 물체 쪽이 망가져도 키보드로 통과하던 이중 경로였다.
- PlayMode 하네스 신설 — `IInteractable.Interact()`를 부른다(= 클릭과 같은 경로).
  **26 PASS / 0 FAIL**

## WP-D·F — 계기판 + 위험 상태  ✅  (커밋 `5a920af`)

- `RiskEvaluator` (순수 C#, 히스테리시스), `RiskProfile` 3종 프리셋, `RiskStateView`가
  조명·경고등·물체 진동·절차생성 험 **네 채널**을 동시에 민다.
- `InstrumentPanelView` — `ElevatorGrayboxView`가 폐기된 `RunController`를 참조해 죽어 있던
  자리를 대신한다. 전력 게이지에 100/130/170/220% 눈금.

### PlayMode 검증이 잡아낸 결함 — 과수확 가중치

과수확 가중치 2.6에서 **"과수확 1회 후에도 Stable"** 로 실패했다.
`MASTER_PRD.md` §7이 과수확을 "공간적 사건"으로 규정하므로 이건 튜닝이 아니라 명세 위반이다.
Warning 진입선 위(3.2)로 올리고 `OverharvestWeight >= WarningEnter` 불변식을 테스트로 고정.

## WP-G — 사고 기록기  ✅  (커밋 `cc32376`)

`RunSession`은 확정 즉시 `Current`를 교체하므로 확정 뒤에 물어보면 이미 없다.
보고 있는 층을 직접 붙들고 있다가 `Result`가 채워지는 프레임에 기록한다.
층 중간의 **최고** 위험 단계를 누적한다 — 끝난 시점의 위험만 남기면 실패 원인이 사라진다.
**PlayMode 34 PASS / 0 FAIL**

## WP-I — 캡처 · 성능  ✅  (커밋 `947588b`)

### 고정 캡처가 잡아낸 결함 3건

1. **하이라이트가 심볼을 6배로 부풀렸다.** `child.localScale = Vector3.one * k`가 씬에서
   저작된 스케일(구 0.17 / 정육면체 0.16 / 캡슐 0.15)을 덮어썼다. 결과판 전체를 가리는
   흰 판때기가 캡처에 찍혔다. → 저작 스케일을 캐시해 **곱한다**.
2. **과수확 명판이 벽 하나를 덮고 좌우가 뒤집혔다.** fontSize를 월드 단위로 계산하고
   Y 180°를 걸었다. → 기존 계기판 라벨과 같은 규약(스케일 0.05 / fontSize 10, 회전 없음).
3. **계기판 3줄 상태 텍스트가 전력 게이지를 덮었고 "위험 위험"으로 읽혔다.**
   → 2줄 상한 + "위험도" 라벨 + 라벨 세로 간격 재배치.

캡처 없이 자동 테스트만 돌렸으면 셋 다 통과했을 결함이다.

### 고정 시드 3개

| 시드 | 용도 | 확인된 결과 |
|---|---|---|
| **1337** | 기준 진행 | 흡수체 계약, 3스핀에 511/460 달성, 과수확 1회 → 558.42 (정상 상승) |
| **7** | Critical 도달 | 증식체 계약, 과수확 2회 → 위험 점수 8.8, 전력 4008/460 |
| **12** | 깊은 연쇄 | 증식체 계약, 연쇄 6단계 (캡처는 4단계 시점) |

### 캡처 세트 (`Captures/HeroSlice/`, 1920×1080 / FOV 60 / 전용 카메라 RT)

`01_interior` `02_board_front` `03_contract_select` `04_stable` `05_critical`
`06_overharvest_access` `06b_overharvest_locked_compare` `07_cascade_deep` + `manifest.txt`

**Stable(04)과 Critical(05)은 동일 좌표·FOV·해상도**다 — 무음 비교의 전제.

### 성능 (참고치, 성능 완료 선언 아님)

기기: Apple M5 / Metal / macOS 26.5.2. 에디터 Play 모드, 게임뷰 979×485.
`TECH_SPEC.md` §13 기준 PC(Ryzen 7 5700 / RTX 3070 / Windows)가 아니므로
§13에 따라 **성능 완료를 선언하지 않는다.**

| 항목 | 값 |
|---|---|
| 판정 순수 비용 | 스핀당 **7.7 µs** / **336 B** (1000스핀, 연출·렌더 없이) |
| 유휴 프레임타임 | 중앙 0.84 ms / 95% 1.08 ms / 최악 1.62 ms |
| 스핀·캐스케이드 중 | 중앙 1.22 ms / 95% 4.58 ms / **최악 200.57 ms** |
| 유휴 GC Alloc | 프레임당 평균 **143 KB** |
| 유휴 GC Alloc — IMGUI HUD 끔 | 프레임당 평균 **15.6 KB** |

**§13의 "프레임당 0 B GC Alloc 목표"를 충족하지 못한다.**
유휴 143 KB 중 **127 KB(약 89%)가 IMGUI 디버그 HUD**다. 남은 15.6 KB는 뷰의 매 프레임
문자열 생성으로 추정된다. 판정 자체는 무관하다.
**최악 프레임 200 ms 스파이크는 원인 미확정** — GC 수집으로 추정되나 확인하지 않았다.

### 빌드

- **Windows x86-64 빌드 불가** — 이 기기에 Windows Build Support 모듈이 설치되어 있지 않다
  (`BuildPipeline.IsBuildTargetSupported(StandaloneWindows64) == false`). 명세 대상 플랫폼이므로
  차단 원인으로 보고한다.
- **빌드 씬 목록이 잘못돼 있었다.** `Assets/Scenes/SampleScene.unity`를 가리키고 있어, 그대로
  빌드하면 프로토타입이 들어 있지 않은 실행 파일이 나왔다. `Prototype_Elevator.unity`로 교정.
- **macOS 개발 빌드는 성공했다** (309 MB, `Build/macOS/Ascend.app`, gitignore 대상).
  대체 산출물이 아니라 **"플레이어 빌드에서 코드가 컴파일되는가"** 를 확인하기 위한 것이다 —
  에디터 전용 API가 런타임 코드로 새지 않았음이 증명된다.
- 빌드가 남긴 부수 변경(URP 프리필터 플래그, `ProjectSettings`, TMP 폰트 아틀라스 스트립,
  **UnityConnect 분석 활성화 0→1**)은 전부 되돌렸다. 씬 목록 교정만 커밋한다.
  `CLAUDE.md`가 `Assets/Settings/`를 단일 소유로 두고 URP 변경에 승인을 요구하기 때문이다.

---

# 세션 종료 상태

| Gate | 판정 | 근거 |
|---|---|---|
| A — 코어 | **통과** | EditMode 49 PASS / 0 FAIL. 씬 없이 통과, 동일 시드 재현, 캡 20 미초과가 전부 테스트로 고정 |
| B — 플레이 흐름 | **통과** | PlayMode 34 PASS / 0 FAIL. 디버그 조작 없이 1층 완주 |
| C — 판독성·위험 | **부분 통과** | Stable/Critical 무음 구분 ✅, 심볼 3종 실루엣 구분 ✅, 레버 혼동 없음 ✅ / 정화 원인 하이라이트는 약함 |
| D — 기술 증거 | **부분 통과** | 콘솔 오류 0, 시드·캡처·성능 제출 ✅ / GC Alloc 목표 미달, Windows 빌드 차단 |

## 다음 세션 시작점

1. **GC Alloc** — IMGUI 디버그 HUD를 실제 UI(월드 스페이스 또는 UGUI)로 교체하면 유휴 143 KB 중
   127 KB가 사라진다. 남은 15.6 KB는 뷰의 매 프레임 문자열 생성.
2. **최악 프레임 200 ms 스파이크** — 원인 미확정. 재현: `Ascend/Measure Hero Slice Performance`.
3. **정화 원인 하이라이트(B-2.6)** — 현재 맥동 횟수로만 구분한다. 정지 캡처에서는 개수/직선/연결이
   구분되지 않는다. 직선에는 선, 연결 덩어리에는 테두리 같은 **형태** 표식이 필요하다.
4. **계약 없음이 열등 선택지** — 잔류 대가가 출현률·보상 증가를 상쇄하지 못한다(위 측정표).
5. **폐기 코드 약 5,000줄** (`Core/` `Roulette/` `Effects/` `Data/Ball*` `Sim/` `UI/PrototypeUI`,
   `Assets/Editor/`의 구 하네스) — 컴파일에는 참여하나 아무도 쓰지 않는다. 별도 제거 티켓.
6. **과수확 잠금 해제 연출** — 현재 덮개가 열리기만 한다. `VISUAL_SPEC.md` §7의 "조명·소리·기계
   반응이 집중된다"는 아직 없다.

---

## 후속 세션 — GC Alloc 과 정화 표식 (커밋 `539ce72`, `622b4fd`)

### 1. 정화 원인 형태 표식 (남은 항목 3번)

문제: 맥동 **횟수**로만 패턴을 구분하고 있어서 정지 캡처에서는 개수·직선·연결이 전부 같아 보였다.
`.claude/visual-criteria.md` B-2.6 "전부 같은 이펙트면 실패다"에 걸린다.

막힌 지점은 데이터였다. `PurifyEvent.Cells`는 **정화된 칸 전부**이고, 어느 칸이 직선을
이뤘는지·어느 칸이 덩어리였는지는 엔진 내부에만 있었다. 뷰가 보드를 다시 훑어 찾는 것은
판정의 두 번째 구현이므로 금지다(`TECH_SPEC.md` §5).

| 변경 | 내용 |
|---|---|
| `PurifyEvent.PatternCells` 신설 | 패턴을 이룬 칸. `Cells`(정화된 전부)와 구분된다 |
| `TryFindLine` | bool → **그 줄의 3칸**을 반환. `SpinBoard.Lines` 원본을 복사해 내보낸다 |
| `HasConnectedComponent` → `FindConnectedComponent` | bool → **가장 큰 덩어리의 칸**을 반환 |
| `PurifyMarkerView` 신설 | 막대 풀 28개를 미리 잡고 형태를 그린다 |

형태 규약:

| 패턴 | 표식 |
|---|---|
| 개수 정화 | 없음 — 흩어져 있다는 것 자체가 그림이다 |
| 직선 | 세 칸을 **관통하는 긴 막대 하나** |
| 연결 | 붙어 있는 칸 **사이를 잇는 짧은 연결봉들** (규칙을 그대로 그린다) |
| 잭팟 | 9칸이 이어져 격자 그물이 된다 |

검증: `08_pattern_line`(대각선 [0,4,8] 관통 막대)과 `07_cascade_deep`(연결봉 그물)이
같은 시점·해상도에서 명백히 다른 모양으로 찍혔다. 테스트 5종 추가(총 54 PASS).

**남은 한계**: 대각 연결이 해금되면(`DiagonalCountsAsConnected`) 대각 연결봉도 그려야 한다.
현재는 직교만 잇는다 — 그것이 기본 규칙이기 때문이다.

### 2. GC Alloc (남은 항목 1번)

#### 측정 방법을 먼저 고쳤다

직전 보고의 "유휴 143 KB/프레임"은 **평균**이었고, 에디터가 이따금 섞는 22 MB짜리 단발
프레임 하나가 180프레임 평균을 124 KB 밀어올린다. "게임 루프가 매 프레임 할당하는가"는
평균이 답할 수 없는 질문이다. 중앙값·95%·최대·**0 B 프레임 비율**로 바꿨다.

또 게임 쪽 MonoBehaviour를 **전부 끈 대조군**을 추가해, 남은 바닥이 우리 코드인지
에디터·URP인지 분리했다.

#### 결과 (중앙값 기준, Apple M5 / Metal / 에디터 Play)

| 조건 | GC/프레임 중앙 |
|---|---|
| 전체 | **12,615 B** |
| IMGUI 디버그 HUD 끔 | 10,411 B |
| 게임 뷰 컴포넌트까지 전부 끔 | **8,773 B** |

- **HUD 기여: 2,204 B** (이전 약 127,000 B → **98% 감소**)
- 나머지 게임 코드 기여: 1,638 B
- **게임 코드 합계: 3,842 B** (이전 약 134,000 B → **97% 감소**)
- **에디터·URP 바닥 8,773 B는 우리 코드가 아니다** — 게임 컴포넌트를 전부 꺼도 남고,
  중앙값과 95%가 같다(상태와 무관하게 일정하다).

#### 고친 다섯 곳

| 위치 | 무엇이 문제였나 |
|---|---|
| `RouletteHud` | `OnGUI`는 프레임당 Layout·Repaint로 **최소 2회** 불린다. 그 안에서 상태 문자열을 매번 조립했고 `GUILayout`은 호출마다 레이아웃 항목을 할당한다 → 고정 Rect + 재사용 `GUIContent` + 변경 시에만 재조립 |
| `CrosshairInteractor` | `GetComponentsInParent`(**복수형**)가 호출마다 배열을 만든다. 매 프레임 스피어캐스트 히트마다 불려 프레임당 최대 16개 → 단수형 인터페이스 조회 |
| `InstrumentPanelView` | 매 프레임 라벨 3줄을 보간 문자열로 조립 → `StringBuilder` + TMP의 `SetText(StringBuilder)` 오버로드(중간 string 없음) + 표시값 변경 시에만 |
| `RouletteInteractionBridge` | 매 프레임 프롬프트 3개를 조립 → 상태 키가 바뀔 때만 |
| `RiskStateView` | 매 프레임 `Explain()`(List + `string.Join`) → 위험 요인 구성이 바뀔 때만 |

#### 아직 남은 것

- **`TECH_SPEC.md` §13의 "프레임당 0 B" 는 에디터에서 확인할 수 없다.** 에디터·URP 바닥
  8.8 KB가 게임 코드 기여분(3.8 KB)보다 크기 때문이다. 0 B 주장은 **플레이어 빌드
  프로파일링**으로만 가능하다.
- 게임 코드에 남은 3.8 KB의 세부 출처는 특정하지 않았다. 남은 후보는 IMGUI 자체의
  이벤트 처리(디버그 HUD를 실제 UI로 교체하면 사라진다)와 뷰의 `MaterialPropertyBlock` 경로다.
- **최악 프레임 스파이크는 그대로다** (중앙 0.91 ms 대비 최악 130 ms, 143배).
  원인 미확정 — GC 수집으로 추정되나 확인하지 않았다.

---

## 프레임 스파이크 원인 규명 (커밋 예정)

직전 보고의 "최악 프레임 130 ms, 중앙의 143배 / 원인 미확정"을 계측으로 좁혔다.
가설을 세우고 **하나씩 반증**하는 순서로 진행했다.

### 계측 설계

프레임타임만으로는 "왜 느렸는가"를 절대 알 수 없다. 그래서 느린 프레임과 **같은 프레임의**
정황을 함께 찍었다: GC 세대별 누적 수집 횟수, TMP 동적 아틀라스 글리프 수·텍스처 장수,
연출 단계, 스핀 입력 여부.

### 가설 1 — TMP 동적 아틀라스 확장 → **반증**

`NanumGothic SDF`가 `atlasPopulationMode: Dynamic`이고, 폰트 에셋이 이전 커밋들에서
플레이 모드 중 계속 수정돼 있었다(런타임에 글리프가 추가된 흔적). 가장 유력해 보였다.

측정 결과 **모든 스파이크에서 글리프 증가 0**:

| 실행 | 기준 초과 프레임 | 그중 글리프 증가 |
|---|---:|---:|
| 1회차 | 37개 | **0개** |
| 2회차 | 33개 | **0개** |
| 3회차 | 31개 | **0개** |

글리프 수도 측정 전후 171개로 동일. 아틀라스도 2장 그대로. **기각.**

### 가설 2 — 연출(캐스케이드 재생)이 무겁다 → **반증**

시드 12를 고정해 **같은 연쇄를 세 번** 재생하며 컴포넌트를 하나씩 껐다.

| 조건 | 프레임 | 중앙 | 최악 | 스파이크 | 최대 할당/프레임 |
|---|---:|---:|---:|---:|---:|
| ① 전부 켬 | 3,978 | 1.14 ms | 4.6 ms | **0개** | 0.1 MB |
| ② HUD 끔 | 3,915 | 1.17 ms | 2.6 ms | **0개** | 0.1 MB |
| ③ HUD + 표식 끔 | 3,833 | 1.18 ms | 8.1 ms | **0개** | 0.1 MB |

**통제된 재생에서는 스파이크가 아예 나오지 않는다.** 최대 단일 프레임 할당도 0.1 MB로,
문제 구간에서 보이던 5~18 MB와 두 자릿수 배 차이다. 연출·표식·HUD 어느 쪽도 아니다.

정착 상태(스핀 이후 300프레임)도 마찬가지로 깨끗하다 — 최악 1.63 ms, 중앙의 1.4배.

### 가설 3 — 에디터 환경의 GC 수집 → **확인됨**

게임 코드를 **전부 끄고** 60초를 재는 대조군에서도 스파이크가 그대로 나온다:

| 대조군(게임 코드 전부 끔) | 값 |
|---|---|
| 프레임타임 중앙 | 1.26 ms |
| **최악** | **45.6 ms** (중앙의 36배) |
| 최대 단일 프레임 할당 | **3.15 MB** |
| 최악 프레임 2개의 정황 | 할당은 baseline(8,773 B)인데 **gen0/1/2 동시 수집** |

가장 느린 프레임들이 "할당은 평소와 같은데 전 세대 GC가 돌았다"는 형태다. 이건 정의상
**GC 일시정지**다. 게임 코드가 하나도 안 돌고 있어도 발생한다.

### 결론

1. **프레임 스파이크는 게임 루프가 만들지 않는다.** 같은 연쇄를 통제 재현하면 0개다.
2. **가장 큰 스파이크는 전 세대(gen2) GC 일시정지다.** 게임 코드를 전부 꺼도 45 ms가 나온다.
3. **TMP 동적 아틀라스는 원인이 아니다.** 모든 스파이크에서 글리프 증가 0.
4. 다만 **우리 코드도 GC 빈도에 기여한다** — 프레임당 약 11 KB를 꾸준히 할당하므로 gen0을
   계속 채운다. 그중 8.8 KB는 게임 코드를 전부 꺼도 남는 에디터·URP 바닥이고,
   게임 코드 몫은 약 2.5 KB(대부분 IMGUI 디버그 HUD)다.

### 남은 정직한 한계

- 처음 관측한 **131 ms 스파이크는 통제 재현으로 되살리지 못했다.** 그 구간은 앞선 측정
  단계들이 힙을 충분히 어지럽힌 뒤였고, 누적된 힙에 대한 gen2 수집으로 보는 것이 가장
  일관된 설명이다. 그러나 "그 한 프레임이 확실히 gen2 때문이었다"를 단정하지는 않는다.
- **에디터 측정으로는 여기까지가 한계다.** GC 일시정지의 실제 크기와 빈도는 빌드에서
  다르고, 에디터 바닥(8.8 KB/프레임)이 게임 몫(2.5 KB)보다 크기 때문에
  "0 B 목표" 판정도 빌드에서만 가능하다. → 기준 PC(Windows) 확보 시 재측정 필요.

### 조치

원인이 게임 코드가 아니므로 **코드는 고치지 않았다.** 근거 없는 최적화를 넣는 대신
계측 하네스를 저장소에 남겼다(`Ascend/Measure Hero Slice Performance`) — 다음 세션이
빌드에서 같은 구분(게임 코드 / 에디터 바닥 / GC 일시정지)을 바로 다시 낼 수 있다.

---

## IMGUI 디버그 HUD → UGUI 실제 UI 교체

### 무엇을 화면에 남길 것인가부터 정했다

단순 이식이 아니다. 옛 IMGUI HUD는 층·전력·요구·게이지·계약·잔류·스핀·결과판 미러를
한 화면에 전부 쏟았고, 그건 두 조항을 어긴다:

- `MASTER_PRD.md` §6.1 — "한 화면에 모든 숫자를 동시에 띄우지 않는다.
  큰 배수 하나, 현재 캐스케이드 단계, 핵심 발동 원인을 우선 강조한다."
- `VISUAL_SPEC.md` §5 — "상호작용물은 현대적 HUD처럼 떠 보이면 안 된다."

그래서 역할을 나눴다. **화면에는 공간이 담을 수 없는 것만 남긴다.**

| 정보 | 어디서 말하는가 |
|---|---|
| 전력·요구·임계점·스핀·잔류·계약 | **벽면 계기판** (`InstrumentPanelView`) — 이미 있다 |
| 지금 무엇을 할 수 있는가 | **조준 프롬프트** (`CrosshairView`) — 이미 있다 |
| 지금 무엇 때문에 터졌는가 | 화면: 연쇄 단계 + 원인 한 줄 (연출 중에만) |
| 층이 왜 그렇게 끝났는가 | 화면: 사고 기록기 요약 + 재현 시드 |
| 다음 행동 안내 | 화면: 아래 가운데 **한 줄** (숫자 없음) |

### 신규

| 파일 | 역할 |
|---|---|
| `UI/GameHudView.cs` | 게임 화면 UI. 안내 한 줄 / 연쇄 배너 / 층 결과. CanvasGroup 페이드 |
| `UI/DebugPanelView.cs` | **분리된** 디버그 패널. **기본 꺼짐**([F1]로 토글) |
| `UI/RouletteHud.cs` | **삭제** — 씬에서도 제거 |

디버그 패널을 분리하고 기본으로 끈 이유: `VISUAL_SPEC.md` §5에서 "보조 수치와 디버그 정보"는
6순위(맨 끝)다. 옛 HUD는 이걸 게임 정보와 한 덩어리로 묶고 기본 ON이라 캡처·성능 양쪽에
상시로 끼어들었다.

시드 입력에 `TMP_InputField`를 쓰지 않는다. 1인칭이 커서를 잠그고 있어 입력 필드에 포커스를
주려면 잠금을 풀어야 하고, 그러면 조사하려던 상태가 바뀐다. Input System의 `onTextInput`으로
직접 받으면 커서를 건드리지 않는다.

### 결과 — 프레임당 GC 할당

정착 상태(스핀 이후 300프레임) 중앙값 기준:

| 조건 | GC/프레임 |
|---|---:|
| 화면 UI 켬 | **8,853 B** |
| 화면 UI 끔 | 8,853 B |
| 게임 코드 전부 끔(에디터 바닥) | 8,773 B |

- **화면 UI 기여: 0 B/프레임** (옛 IMGUI HUD는 2,204 B, 최초에는 약 127,000 B)
- **게임 코드 전체: 80 B/프레임** (최초 약 134,000 B → **99.9% 감소**)

`SetText(StringBuilder)` + 표시값이 바뀔 때만 재조립 + CanvasGroup 알파(활성 토글 아님)로
정착 상태에서 문자열·메시 재생성이 아예 일어나지 않는다.

**단, "유휴(계약 선택 대기)" 구간 수치는 이번에도 신뢰할 수 없다** — Play 진입 직후라
TMP가 새 UI의 한글 글리프를 처음 래스터라이즈하고 에디터가 가장 시끄러운 시점이다.
실제로 그 구간은 "UI 끔"이 "UI 켬"보다 할당이 **더 크게** 나왔다(불가능한 결과).
정착 상태 수치만 근거로 쓴다.

### 검증

- PlayMode **43 PASS / 0 FAIL / 콘솔 오류 0** — UI 항목 8개 신규
  (존재·기본 꺼짐·안내 표시·연쇄 즉시 표시·연출 후 사라짐·결과 표시·안내 사라짐)
- EditMode 54 PASS / 0 FAIL (회귀 없음)
- 캡처 `09_screen_ui_hint`, `10_screen_ui_cascade` 추가

### 검증 중에 잡은 결함 2건

1. **연쇄 배너가 페이드로 떴다.** 첫 단계의 정화 맥동이 0.55초인데 페이드가 0.17초를 먹어
   "무엇 때문에 터졌는가"를 읽을 시간을 그만큼 깎았다. → **뜰 때는 즉시, 사라질 때만 페이드.**
2. **검증이 프레임 수로 기다리고 있었다.** 에디터가 1000fps로 도는 바람에 "30프레임 대기"가
   0.03초가 되어 페이드가 끝나기 전에 검사했다. → 시간 기준 대기(`WaitSeconds`)로 교체.

### 캡처 경로에 대한 주의

**Screen Space Overlay 캔버스는 카메라의 RenderTexture에 렌더되지 않는다.** 고정 시점
캡처 세트(전용 카메라 → RT, 1920×1080)에는 화면 UI가 절대 나오지 않는다.
그래서 UI 확인용으로 `ScreenCapture` 경로를 따로 뒀고, 해상도가 게임 뷰 크기라
**고정 비교 세트가 아니다**(매니페스트에도 그렇게 적었다).

---

## 과수확 레버를 "대표 장면"으로 — Gate C 마감

Gate C에 남아 있던 마지막 항목. `MASTER_PRD.md` §7이 이 레버를 대표 장면으로 지정했는데
캡처에서는 그냥 회색 상자였다.

### 발견 1 — `Unlocked` 이벤트에 구독자가 없었다

`InteractableOverharvestLever.Unlocked`를 만들어두고 **아무도 듣지 않았다.** 그래서
`VISUAL_SPEC.md` §7의 "해제 순간 조명, 소리, 기계 반응이 집중된다"가 통째로 빠져 있었다.
덮개만 조용히 열리고 끝났다.

`View/OverharvestUnlockEffect.cs` 신설 — 해제 순간 네 채널이 동시에:

| 채널 | 내용 |
|---|---|
| 조명 | 전용 스폿이 확 켜졌다가(0.18s) 낮게 가라앉는다(0.9s) |
| 기계 | 실내등이 45% 어두워졌다 돌아온다 — 전력을 이쪽으로 뺏긴 것처럼 |
| 소리 | 걸쇠 풀리는 둔탁한 소리 + 올라가는 톤 (절차 생성, 외부 에셋 없음) |
| 진동 | 레버 덩어리가 짧게 떨린다 |

그리고 풀린 **동안** 경고 띠가 계속 발광한다 — 잠김/해제가 정지 화면에서도 구분되도록.

### 발견 2 — 덮개가 손잡이를 가린 적이 없다 (캡처가 잡음)

주석은 "덮개가 닫히면 손잡이가 실제로 가려진다"고 주장했지만 캡처에서 잠김 상태인데
손잡이가 그대로 보였다. 깊이를 계산해 보니 **덮개가 손잡이보다 0.22m 뒤**에 있었다.

두 번 고쳤다:
1. 덮개를 앞으로 옮겼다 → 이번엔 0.022m 만 앞이라 손잡이가 덮개를 **관통**했다.
2. 하우징을 오목한 함(깊이 0.52)으로 만들고 손잡이를 그 안에 넣었다.
   덮개 뒷면 −0.405 / 손잡이 앞면 −0.367 → 0.038m 여유로 확실히 가려진다.

경고 띠도 덮개 안쪽(0.44 폭)에 있어서 닫히면 같이 가려졌다. 덮개 **바깥쪽**
(x=±0.26)으로 옮겨 항상 보이게 했다. 잠금등도 덮개 아래로.

### 발견 3 — 실내등 조광이 아무 일도 하지 않았다

`Light.intensity`에 배수를 곱했는데, `RiskStateView`가 매 `LateUpdate`마다 절대값으로
다시 쓴다. 코루틴은 `LateUpdate`보다 **먼저** 도므로 곱한 값이 같은 프레임에 덮어써졌다.
"기계 반응" 채널이 코드상으로는 있는데 화면에는 없던 것이다.

→ `RiskStateView.CabinLightMultiplier`를 두고 소유자를 하나로 만들었다.
추가로 `OnDisable`에서 배수를 반드시 1로 되돌린다 — 연출 도중 컴포넌트가 꺼지면
배수가 0.55인 채로 굳어 **방이 영구히 어두워진다.**

### 검증

PlayMode **48 PASS / 0 FAIL / 콘솔 오류 0**. 신규 항목 5개:
해제 연출 존재 · 해제 순간 재생됨 · 풀린 동안 띠 발광 · 해제 순간 실내등 어두워짐 ·
연출 후 실내등 복구.

캡처 `05b_overharvest_locked` / `06_overharvest_access`는 **같은 좌표**다:

| | 잠김 | 해제 |
|---|---|---|
| 덮개 | 닫힘 — 손잡이 완전히 가림 | 위·뒤로 젖혀짐 |
| 경고 띠 | 하우징보다 어두움 | 주황 발광 |
| 잠금등 | 꺼짐 | 켜짐 |
| 조명 | 없음 | 전용 스폿이 하우징을 비춤 |

### 남은 폴리시 항목

- 해제 후 **손잡이 자체가 어둡다.** 하우징 안쪽에 있어 스폿이 깊이 닿지 않는다.
  상태 구분(Gate C 기준)은 성립하지만 "지금 당길 수 있다"가 더 선명하면 좋다.
- 최종 강도·색·타이밍은 승인 대기 항목이라 전부 인스펙터 값으로 남겼다.
