# OVERNIGHT_ASSUMPTIONS

1차 프로토타입 자율 구현 중 문서에 없던 항목을 직접 결정한 기록.
전부 **테스트용 임시값**이며 최종 밸런스가 아니다.
모든 수치는 `PrototypeConfig` / `CombinationConfig` / `EffectDefinition` /
`PassengerDefinition` 에셋에서 Inspector로 조절할 수 있다.

작성 시작: 2026-07-28
기준 문서: `99. 프로토타입 범위와 미결정 사항` > 개발 티켓 > 01~04·07번 문서

---

## 1. 작업 환경

| 항목 | 결정 | 근거 |
|---|---|---|
| 프로젝트 루트 | `B:\Projects\Upandup_DDD` | 사용자가 직접 지정. iCloudDrive·워크트리 경로에 동일 구조가 있어 혼동 주의 |
| 역할 분담 | 설계·검증 = Claude / 구현 = Codex | 사용자 지시("코덱스랑 협업해서 만들어") |
| Gemini | **사용 불가** | CLI 인증 티어 미지원(`IneligibleTierError`). 독립 검토는 Claude가 직접 수행 |
| 작업 브랜치 | `main` 위에서 작업, 티켓 단위 커밋 | 저장소가 clean 상태였고 사용자 미커밋 변경 없음 |

## 2. 효과 처리기 (T-03)

| 항목 | 결정 | 근거 |
|---|---|---|
| 전력 공식 | `(구슬출력합 + 조합기본점수 + FlatBonus) × 조합배수 × MultiplierBonus` | 02번 문서 "+3 이후 ×2와 ×2 이후 +3은 다르다" — 가산을 먼저, 배수를 나중에 |
| 고정 처리 순서 | `Probability → Remove → Convert → Copy → Add → Multiply → Repeat` | 구슬 리스트를 먼저 확정한 뒤 수치를 계산해야 예측 가능. 변환된 좋은 구슬을 복사할 수 있게 Convert를 Copy보다 앞에 둠 |
| 같은 타입 내 순서 | `priority` 오름차순, 동률이면 등록 순서(안정 정렬) | 같은 시드에서 같은 결과가 나와야 함 |
| **Repeat 의미** | 파이프라인을 **복제본에서 한 번 더 실행해 그 전력을 더한다**. 원본 누산기를 재오염시키지 않는다 | 초기 구현은 같은 컨텍스트를 재실행해 ×2 효과가 ×4로 폭주했다. 도박사 재발동 1회 = 정확히 전력 2배가 직관에 맞음 |
| 반복 패스 내 Repeat | 발동하지 않음 | 재귀 폭주 방지 |
| 무한 루프 방어 | 3중: `maxRecursionDepth=3`, `maxTotalActivations=64`, `(효과id, 깊이)` 순환 감지 | 티켓 요구사항 |
| 상한 도달 시 | 예외를 던지지 않고 경고 로그 후 정상 반환 | 프로토타입이 죽으면 안 됨 |
| `BaseOutputSum` | 저장 필드가 아니라 `Balls`로부터 **계산되는 프로퍼티** | Copy/Remove/Convert가 수동 증감하면 반드시 어긋남 |
| Probability 효과 | 파이프라인에서 전력을 바꾸지 않고 `PendingProbabilityModifiers`에 적재 | 구슬 등장 확률은 추첨 **이전에** 적용되어야 하는 값이므로 |
| 난수 | 전부 `System.Random` 시드 기반. `UnityEngine.Random` 금지 | 재현성 요구 |
| 시드 유도 | 구슬 `seed` / 효과 `seed*397^0x5EED` / 승객 `seed*31^0x9A55` / 사고 `seed*17^0x2ACC` | 같은 시드를 그대로 쓰면 네 시스템이 동일 수열을 공유해 통계가 왜곡됨 |

## 3. 승객 (T-04)

문서에 승객 5종의 수치가 티켓에만 있어 그대로 채택했다.

| id | 이름 | 무게 | 효과 |
|---|---|---:|---|
| PSG_TECHNICIAN | 기술자 | 2 | 발전마다 전력 +2 |
| PSG_TRANSFORMER | 변압기 기사 | 4 | 최종 전력 ×2 |
| PSG_GAMBLER | 도박사 | 3 | 완벽 정지 시 효과 재발동 |
| PSG_PORTER | 짐꾼 | 2 | 허용 중량 +5 |
| PSG_ZEALOT | 과적 광신도 | 6 | 과적 상태에서 전력 ×2 |

| 항목 | 결정 | 근거 |
|---|---|---|
| 요구 전력 공식 | `(기본 + 층×증가량 + 총무게×2) × (과적이면 배수)` | 티켓의 `기본 + 총무게×2` 에 층 증가(T-07)를 합성 |
| 탑승 슬롯 | 6 | 99번 문서 "탑승 슬롯 6개" |
| 층당 승객 후보 | 2 | 프로토타입 단순화. 선택의 긴장은 유지하되 UI를 가볍게 |
| 승객 효과 표현 | T-03 `EffectDefinition`을 그대로 재사용 | 별도 효과 시스템을 만들면 두 벌을 유지해야 함 |
| **완벽 정지 정의** | 수확 구멍 중심으로부터 `perfectStopTolerance`(0.12) 안에서 멈춘 통관이 **셋 다**일 때 | 문서에 정의가 없었음. 타이밍 숙련이 실제로 보상받아야 도박사가 의미를 가짐 |
| 승객 하차 | 프로토타입에서는 구현하지 않음 | 티켓 제외 범위. 02번 문서의 하차 흐름은 1차 범위 밖 |

## 4. 과적 사고 (T-06)

| 항목 | 결정 | 근거 |
|---|---|---|
| 초과 중량 | `max(0, 총무게 - 허용중량)` | |
| 사고 확률 | `min(0.75, 초과중량 × 0.06)` | 초과 12에서 약 72%. 과적 빌드가 도박이 되되 확정 파멸은 아니게 |
| 사고 손실 | 현재 전력의 35% | 층을 즉시 실패시키지 않고 "재시도로 만회 가능"한 수준 |
| 판정 시점 | `PowerResolution`에서 전력 비교 **직전** 1회 | 발전을 다 끝낸 뒤 결과가 흔들려야 긴장이 생김 |
| 확률 공개 | `ElevatorState.AccidentChance`로 상시 노출, HUD에 표시 | 99번 문서 "불합리한 랜덤이 아니라 감수한 위험으로 느껴지는가" |
| 과적 시 요구 전력 | 추가로 ×1.5 | 기존 config 값 유지 |
| **허용 중량** | **100 → 8로 변경** | 승객 5종 총무게가 17이라 허용 100에서는 과적이 **구조적으로 불가능**했고 T-06이 죽은 콘텐츠였다. 8이면 세 번째 승객부터 위험이 시작되고 짐꾼(+5)이 이를 되돌린다 |

허용 중량 8 기준 실측 과적 확률:

| 탑승 | 총무게 | 허용 | 초과 | 사고 확률 |
|---|---:|---:|---:|---:|
| 기술자 | 2 | 8 | 0 | 0% |
| 기술자+변압기 | 6 | 8 | 0 | 0% |
| 기술자+변압기+도박사 | 9 | 8 | 1 | 6% |
| 위 + 짐꾼 | 11 | 13 | 0 | 0% |
| 전원 탑승 | 17 | 13 | 4 | 24% |

## 5. 초과 전력 분배 (T-05)

| 항목 | 결정 | 근거 |
|---|---|---|
| 선택지 A (돈) | 초과 전력 전량 → 돈. 1층만 상승 | 티켓 문구 그대로 |
| 선택지 B (추가 상승) | `추가층 = min(3, floor(초과/60))`, 쓰고 남은 전력은 다음 층으로 이월 | 이월이 있어야 "애매하게 남은 초과"가 낭비로 느껴지지 않음 |
| 추가 상승 1층당 전력 | 60 | 기본 요구 전력 100 대비 다소 저렴 — 상승 선택이 실제 유혹이 되도록 |
| 한 번에 최대 추가 상승 | 3층 | 10층 런이 한 방에 끝나지 않게 |
| 전력→돈 비율 | 1.0 | 기존 config 값 유지 |
| 두 선택지 표현 | `OverchargeOption` 구조체로 값과 라벨을 미리 계산해 노출 | "같은 상황에서도 빌드·자원 상태에 따라 선택 가치가 달라진다"를 UI가 비교 표시할 수 있게 |

## 6. 10층 미니 런 (T-07)

| 항목 | 결정 | 근거 |
|---|---|---|
| 목표 층 | 10 | 티켓 |
| 층당 재시도 허용 | 2회 | 초과하면 런 실패. 실패가 실제로 존재해야 긴장이 생김 |
| 실패 조건 | 한 층에서 재시도 한도 초과 | 99번 문서의 "제한 턴 내 목표층 미도달"을 프로토타입 범위로 축약 |
| 성공 조건 | 도달 층 ≥ 10 (추가 상승으로 건너뛰어도 성공) | |
| 재시작 | `[R]` 한 키. `ResetRun()` 하나가 모든 상태·시드·승객·통관을 초기화 | 재시작 경로가 둘이면 반드시 오염됨 |
| 런 종료 후 입력 | `[R]` 외 전부 무시 | 종료 화면에서 상태가 더 진행되면 안 됨 |
| 몬스터·특수층·하강·상점 | **구현하지 않음** | 티켓 제외 범위. 핵심 루프 검증이 우선 |

## 7. 조작 (키 배치)

문서에 키 배치가 없어 직접 정했다. 상태별로 게이트되므로 키를 재사용한다.

| 상태 | 키 |
|---|---|
| PassengerSelection | `[1][2]` 후보 탑승 / `[0]` 건너뛰기 / `[Space]` 진행 |
| GenerationTurn | `[1][2][3]` 통관 1·2·3 정지 / 전부 정지 후 `[Space]` |
| OverchargeAllocation | `[1]` 돈 / `[2]` 추가 상승 / `[Space]` 확정 |
| 상시 | `[R]` 재시작, `[P]` 구슬 확률표 토글 |

`[1][2][3]`이 통관 A·B·C와 1:1 대응한다는 점을 HUD에 항상 명시한다.

## 8. 시각 표현 (그레이박스)

| 항목 | 결정 |
|---|---|
| 구슬 | URP/Unlit 구체 + `debugColor` 틴트 (기존 유지) |
| 등급 색 | 일반=회색 / 고급=하늘색 / 희귀=보라 / 전설=주황 |
| 통관 상태 | `낙하중` / `제동중` / `정지 <구슬id> <등급>` 텍스트 |
| 완벽 정지 | `(완벽)` 표기 + 초록 |
| 과적 | 무게 줄 빨강 + `⚠ 과적 +N` |
| 효과 연쇄 | `EffectResolver.BuildLogText()`를 HUD에 여러 줄로 출력 |

07번 문서의 문양·재질·오컬트 연출은 1차 범위 밖(아트 미제작).

## 9. 자동 테스트 (T-08)

| 항목 | 결정 |
|---|---|
| 시뮬레이션 방식 | 씬·MonoBehaviour 없이 순수 C# `RunSimulator` |
| 공식 공유 | 시뮬레이터가 `FloorMath` / `CombinationEvaluator` / `EffectPipeline`을 **그대로 재사용**. 공식 중복 구현 금지 |
| 런 횟수 | 12회 (요구 최소 10회 초과) |
| 정책 | 경량형 / 균형형 / 과적형 3종으로 빌드 방향별 비교 |
| 완벽 정지 확률 | 정책값 0.25 — 사람의 조작 숙련도를 대신하는 **가정값** |
| 산출물 | `PlaytestLogs/` 에 JSON 1개 + CSV 2개(층 단위·턴 단위), UTF-8 BOM |

**주의:** 시뮬레이터의 `perfectStopChance`는 실제 사람의 조작 숙련도가 아니라
임의 가정값이다. 이 값에 의존한 밸런스 결론은 사람 플레이테스트 전까지 잠정이다.

## 10. 명시적으로 구현하지 않은 것

핵심 루프 검증이 우선이므로 아래는 의도적으로 제외했다(티켓 제외 범위와 일치).

세이브, 네트워크, 결제, 메타 성장, 영구 해금, 상점 밸런스, 캐릭터 애니메이션,
대규모 세계관 콘텐츠, 다수 몬스터·이벤트, 최종 VFX·사운드, 아트 리뉴얼,
유료 에셋, 승객 하차·거래, 하강, 특수 숫자 층, 저주, NPC 목적지 보상.
