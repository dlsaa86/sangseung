# 밤샘 작업 보고 — 2026-08-05 야간

**커밋** `c415733` · 브랜치 `agent/phase2-full-prototype` · 로컬만 (푸시 안 함)
**검증** 86 PASS / 0 FAIL · 컴파일 오류 0 · `FUN_CRITERIA` 전 항목 ✅

---

## 한 줄 요약

오늘자 감사 셋(빌드·계약·적재)이 **전부 같은 하나를 보고 있었다.** 이득은 캐스케이드를
타고 곱셈으로 복리인데 **대가만 선형**이었다. 값이 아니라 **모양**이 문제였고, 세 곳에
같은 처방을 넣었다. 계수를 0으로 두면 셋 다 옛 게임으로 정확히 돌아간다.

---

## 1. 먼저 — Unity 접근 상황

**이 세션에는 끝까지 안 붙었다.** 원인은 찾았다: `unity-mcp` 는 Claude Code CLI 쪽
등록이고, Cowork 는 **데스크톱 앱에 등록된 로컬 MCP만** 프록시한다. 설정 파일에
넣어 뒀지만 이미 맺어진 커넥션이라 이 세션엔 반영이 안 됐다.

→ **새 태스크를 하나 열면 잡힐 가능성이 높다.** 안 잡히면 앱 **설정 → 커넥터**에
Blender와 같은 방식으로 등록하는 경로가 남아 있다.

그래서 이번 작업은 **씬·프리팹·머티리얼·`.asset` 을 한 번도 열지 않았다.**
전부 `.cs` 와 문서다. 모델링 교체에 영향 없다.

---

## 2. 없어서 못 하던 것부터 만들었다

`CLAUDE.md` §7이 「자체 검증을 코드 변경 뒤에 돌리고 커밋한다」를 요구하는데,
그 **유일한 실행 경로가 에디터 메뉴**였다. 에디터가 없거나 컴파일이 깨져 도메인이
리로드되지 않으면 검사를 돌릴 방법이 아예 없었다 — **그때가 검사가 가장 필요한 순간인데.**

```
cd tools/headless && dotnet run -c Release -- tests
  층 루프 (RunTests)          24 PASS / 0 FAIL
  절제·탐욕 (MercyHunger)     13 PASS / 0 FAIL
  스핀 엔진 (SpinEngine)      29 PASS / 0 FAIL
  규칙 다발 (SpinRuleSet)     13 PASS / 0 FAIL
  시뮬레이터 일치             7 PASS / 0 FAIL
  합계 86 PASS / 0 FAIL       ← 1.0초
```

판정을 다시 구현하지 않는다 — 프로젝트의 `RunAll()` 을 그대로 부른다.
**에디터 자체 검증을 대체하지는 않는다**(MonoBehaviour·씬을 잡는 검사는 여기 없다).

측정 도구도 둘 늘렸다. `strategy`(빌드 전략이 실제로 여럿인가) ·
`contracts`(계약이 선택인가 정답인가). 아래 판정은 전부 이 둘이 낸 것이다.

---

## 3. 버그 셋 — 그리고 **변이로 확인했다**

「고쳤다」와 「그 고침이 지켜진다」는 다르다. 셋 다 고친 뒤 **결함을 일부러 되돌려**
새 검사가 실제로 빨간불이 되는지 확인했다.

| 결함 | 되돌렸을 때 |
|---|---|
| **CE-1 확정 단계 영구 교착** (시드 1337) | `FAIL — 요구 전력이 660 로 올라 달성이 무너졌는데 단계가 Decision 다, 네 출구가 모두 닫혔다` |
| **`_residual` 이월** 한 줄 삭제 (587개 중 0개 실패하던 것) | `FAIL — 앞 스핀이 증식체 잔류 +1.2 를 남겼는데 다음 스핀이 잔류 없는 결과와 같다` |
| **`NetPower` 판정식 파괴** | `FAIL — 순전력 98 ≠ 총전력 98 − 잔류 손실 16` |

**CE-1은 요구 전력을 얼려서 고치지 않았다.** 그건 밸런스 결정이다(적재의 대가가 그 층에서
사라진다). 「스핀이 남았는데 요구 전력에 못 미친다」는 상태의 이름은 이미 있다 —
`Spinning`. **단계를 상태의 함수로 되돌리니** 경제는 한 자리도 안 바뀐 채 출구만 열렸다.

덤으로 이름이 틀렸던 것도 잡았다. 흡수체 잔류는 「다음 스핀」이 아니라 **같은 스핀**에서
차감된다(노션 §5.2 판정 순서 9). 다음 스핀으로 넘어가는 건 증식체 쪽뿐이라 검사를 둘로 쪼갰다.

---

## 4. 코어 재미 — 개편 전/후 실측

| 지표 | 전 | 후 |
|---|---:|---:|
| **시너지 ≥ +1%p 짝** | **0개** | **4개** (최대 +2.05%p) |
| 축 간 폭 | 18.70%p | **11.97%p** |
| 「1등 금지」 vs 「전부」 | 18.57%p | **14.07%p** |
| **안정 축 완주율** | 22.60% ← *무적재(23.00%)보다 낮았다* | **27.57%** |
| **패턴 축 완주율** | 23.30% ← *무적재와 사실상 동일 = 없는 축* | **26.20%** |
| 계약 층별 격차 평균 | 14.9%p | **4.7%p** |
| 대가 축 한계비용 (70→80) | ×0.870 (첫 구간 ×0.877과 **같다**) | **×0.530** |
| 과적 | 100→120에서 5.95%→0.07% (**81배 절벽**) | 완충 구간 |
| `FUN_CRITERIA` 3연쇄 이상 비율 | **15.1% ❌** | **14.9% ✅** |

**시너지 4쌍이 나온 이유**는 밸런스 조정이 아니라 구조 변경이다. 모든 효과가 조건 없는
가산·곱셈 한 번이라 **두 품목의 합이 각자의 합과 달라질 경로가 존재하지 않았다.**
값을 아무리 만져도 0개로 유지된다. 조건 축(`BuildEffectCondition`)을 넣으니 나온 4쌍이
전부 **설계한 대로**다 — 사선 결속기+연쇄 조속기(감사가 든 바로 그 예시), 광신자+과수확
변압기, 광신자+중복 계수기, 측량사+중복 계수기.

**3연쇄 비율은 격자 탐색 없이 풀렸다.** 감사는 「3연쇄와 정화 비율을 함께 보는 격자
탐색」을 요구했는데, 두 지표를 밀던 것이 같은 결함(선형 잔류 대가)이었다.

**1등을 깎지 않았다.** 사선 결속기는 값을 그대로 두고 **대가를 붙였다** — 대각을 세는
만큼 덩어리 하나의 값이 내려간다. 「자주 걸리지만 무르다」.

---

## 5. 손대다 **되돌린** 것 (이게 제일 중요할 수도)

증식체 계약의 셋째 값(잔류 대가 ×2)이 곱해지는 대상은 「다음 스핀에 증식체가 더 나온다」다.
증식체는 정화하면 전력이 되므로 **그건 대가가 아니라 재료**다. `ResistanceContract` 자신의
주석이 「셋 중 하나만 바꾸면 계약이 '그냥 좋은 버프'가 된다」고 못박아 둔 그 상태.

고치려고 「남은 증식체가 다음 스핀 정상 영혼 가중치를 깎는다」를 구현해서 재 봤더니 —
**완주율이 오히려 올라갔다** (15.15% → 18.95%).

> 영혼 가중치를 깎으면 판의 **저항 밀도**가 올라가고, 이 게임은 그것을 보상한다.
> 정화는 패턴 배수와 캐스케이드를 타는데 정상 영혼은 안 타기 때문이다.
> 같은 사실이 **영혼 포집망의 개편 전 기여 −3.00%p** 로 이미 나와 있었다. 세 관측이 같은 하나를 가리킨다.

**되돌렸다. 배선도 안 남겼다.** 방향이 반대인 대가를 붙이면 「대가를 넣었다」고 기록되면서
실제로는 이득이 커진다.

---

## 6. 결정해줘야 하는 것 둘

### PD-29 · 계약은 아직 정답이다 — **격차는 줄었는데 답이 안 바뀐다**

7·8·9층 전부에서 **적재 없음 / 흡수체 빌드 / 증식체 빌드 셋 다 증식체 계약이 1등**이다.
노션 §4가 요구하는 「**현재 빌드에 가장 유리한** 계약은 무엇인가」가 데이터로 성립하지 않는다.
격차를 줄이는 것으로는 이 줄이 안 바뀐다 — 격차가 0이어도 답이 하나면 선택이 아니다.

네 가지 안(A~D)을 `PENDING_DECISIONS`에 근거와 함께 적어뒀어. **A(증식체 잔류에 전력 차감)를
자율로 안 한 이유**: 증식체의 성격 자체를 바꾸는 결정이고, 그러면 흡수체와 같아지는 방향으로
간다. 밸런스가 아니라 설계 결정이야.

측정은 준비돼 있어 — `dotnet run -- contracts 3000` 한 줄이 「빌드에 따라 1등이 뒤집힌 층: N개」로
답한다. **그 줄이 0이 아니게 되는 게 완료 조건.**

### PD-30 · 사선 결속기가 여전히 기여의 74%

**깎아서 안 풀린다는 걸 실측으로 확인했어.** 대가를 −0.6 / −1.2 / −1.8로 세 번 재보니
1등 기여는 17.40 → 15.70 → 13.00%p로 선형에 가깝게만 내려갔고 **2등은 3.20%p에서 한 자리도
안 움직였다.** 더 깎으면 품목 정체성만 없어지고 순위는 그대로.

진짜 이유는 `DiagonalCountsAsConnected`가 **밸런스 값이 아니라 판정 규칙**이라는 것.
다른 품목은 전부 배수·확률을 미는데 이것만 「판이 무엇을 연결로 세는가」를 바꾼다.
노션 §3.2가 요구하는 층위라 강한 게 잘못은 아니고 — **그 층위의 품목이 이것 하나뿐인 게 문제**야.

제안: 깎지 말고 **같은 층위를 더 만들자.** 노션이 동결해둔 2종(연쇄 코일 = 캐스케이드 칸
추가 재추첨, 검침원 = 잔류 1개 무효화)이 정확히 그 층위인데 `SpinEngine` 수정이 필요해서
계속 미뤄져 있었어.

---

## 7. 다음 시작점

1. **새 태스크를 열어서 Unity MCP가 잡히는지 확인** → 잡히면 씬 작업 재개
2. **PD-29 결정** (계약을 빌드 의존으로 만들 것인가)
3. PD-30 — 판정 규칙을 바꾸는 품목 2종을 만들 것인가

## 8. 안 건드린 것

- 씬·프리팹·머티리얼·`.asset` **전부**. 워킹 트리에 이미 있던 미커밋 변경
  (`.unity` 1개 · `.mat` 10개 · 폰트 `.asset` 2개)도 그대로 두고 커밋도 안 했어.
- `UP-FIX-100`/`105`/`106`/`107` 등 시각 항목 — 범위 밖
- Mercy/Hunger 배선(`UP-FIX-109`) — Required 승격이 범위 결정이라 손 안 댐

---

### 파일

- 변경: `.cs` 8개 (Build 2 · Spin 3 · Run 2 · Data/Profiles 1) + 하네스 6개
- 실측 산출: `docs/runtime/headless/20260805/` (개편 전/후 대조 10종)
- 문서: `ASSUMPTION_LOG`(A-20260805-01~06) · `PENDING_DECISIONS`(PD-29·30) · `TOPDOWN_PROGRESS`(v10)
