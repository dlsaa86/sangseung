# STORY-CABIN-01 — 블렌더 카 내부 셸 채택

> 상태: In Progress
> 모드: AFK_AUTONOMOUS (사용자 취침, 2026-08-04 야간)
> 브랜치: `agent/phase2-full-prototype`
> 레퍼런스: `docs/references/elevator/mood_20260804_user_cabin.png`

## 사용자 지시 (원문 요지)

> 공간의 분위기가 생각보다 맘에 안 든다. 블렌더로 우선 모델링을 진행하고
> 완성된 모델링을 유니티에 인식하는 순서로 하려고 한다. 이미지는 참고만 —
> 분위기나 형태만 맞추고 나머지는 PRD 읽고 자유롭게 디자인해라.

## 진단 (2026-08-04 기준선 캡처)

`Captures/baseline_20260804/` 4장에서 확인한 것 — **배치와 구조는 이미 레퍼런스와
맞는다.** 가위문 좌측, 장치 정면, 벤치 우측, 케이지등 천장. 실패는 형태가 아니라
아래 둘이었다.

| # | 실패 | 근거 |
|---|---|---|
| 1 | **값이 두 덩어리로 갈라짐** | 벽은 명도 ~15%인데 바닥·천장·천장보·바닥테두리·선반·문설주가 **무텍스처 밝은 회색 ~70%**. 레퍼런스는 모든 면이 서로 몇 % 안쪽이다. 이 한 가지가 「블록아웃에 벽지 붙인 것」으로 읽히게 만든 주범이다. |
| 2 | **면에 음영이 없음** | 벽이 평면 쿼드 + 타일링 텍스처라 그림자가 생기지 않는다. 레퍼런스의 인상은 「오목 패널 + 돌출 스트랩 + 리벳 띠」가 만드는 **실제 음영**이고, 텍스처로는 그게 안 나온다. |

부수 실패: 벽 텍스처가 0.5m 격자로 눈에 띄게 반복됨 / 대기광이 높아 구석이 안 어두움 /
가위문이 황동색으로 너무 밝음 / 천장이 밝아 시선을 끌어감.

→ **그래서 블렌더로 간 것이 옳다.** 면 분할을 진짜 형상으로 만들어야 하는 문제다.

## 범위

### 한다
- `tools/blender/build_cabin.py` — 카 내부 셸 절차적 조립기
- 모듈 9종, `ELV_` 접두사 (`DEVICE_DESIGN_SPEC.md` §7 명명 규칙)
- `Assets/Prototype_Elevator/Art/Models/ELV_Cabin.fbx` 내보내기
- `Assets/Editor/AscendCabinAdoption.cs` — 임포터·재질·배치·조명 채택 (원복 1메뉴)
- 종전 셸 오브젝트 비활성 (삭제하지 않는다 — 원복 가능해야 한다)
- 고정 캡처 + 독립 시각 평가

### 하지 않는다 (범위 밖 — 건드리면 안 된다)
- 수확 장치 구조 (통관 3개 · 3물체 분리 · 3×3 결과판) — `D-20260730-08` 고정
- 룰렛 판정 로직, 층 진행, 승객·부품
- 치수 변경 — `ReferenceRoomSpec.cs` 상수가 정본이다
- 패키지·Unity·URP 버전
- 원격 push, PR, main 병합

## 치수 고정 (ReferenceRoomSpec.cs — 변경 금지)

```
InteriorWidth  4.0   InteriorDepth 4.6   InteriorHeight 2.9   ShellThickness 0.16
GateOpeningWidth 2.1  GateOpeningHeight 2.35
MachineCenterX -0.35  장치 폭은 벽 폭의 45~52%
```

## Acceptance Criteria

- [x] AC1 — 재실행 가능. 치수가 전부 `ReferenceRoomSpec` 미러 상수에서 온다 (치수 명세 테스트 19 PASS 로 불변 확인)
- [x] AC2 — 모듈 **10종**(마모 패스 추가) `ELV_` 접두사 + UV 완비. 23,216 tris
- [x] AC3 — 임포트 오류 0. 재질 6건 `Materials/Cabin/*` 로 리맵
      ⚠ 처음엔 **조용히 실패**했다 — `materialLocation=External` 이 `.meta` 에 남아
      `.mat` 은 Stylized 인데 렌더러는 URP/Lit 을 물었다. `InPrefab` 명시 복원으로 해결
- [x] AC4 — 배치 완료(yaw 180° 흡수). 출입구·장치·레버·동선 확보.
      장치가 벽감에 **사면 105mm 여유**로 안착, 벤치–물품 정합(상판 5mm 안착)
- [x] AC5 — **대역비 13.3× → 8.14× → 2.09× → 1.91×** (목표 ≤5×). 무텍스처 밝은 회색 슬래브 소멸
- [~] AC6 — **패널 릴리프 읽힘. 리벳 릴리프 미달** → `PD-20260804-01` 로 올림.
      값 레버 소진(알베도 상한). 그레이징 광원이라는 **배치 결정**이 필요하다
- [x] AC7 — 컴파일 오류 0, 콘솔 치명 오류 0. Self 556 PASS / EditMode 537 PASS
- [ ] AC8 — 독립 시각 평가 **진행 중** (`visual-critic`)
- [x] AC9 — `Ascend/Cabin/9`(셸) · `8`(값 폭) 둘 다 동작. 값 폭은 원본 색 8종 JSON 백업
- [x] AC10 — 진행 문서 갱신 + 로컬 커밋 6회

## 추가로 달성한 것 (범위 밖이었으나 같은 결함군)

- **문 밖 통로 3.6m** — `DEVICE_DESIGN_SPEC` §2.6 이 미달 판정해 둔 실질 결함 2건 중 하나
- **생 스카이박스 제거** (가위문 너머, 0.00~0.08%)
- **바닥–콜라이더 34mm 어긋남** 해소
- **`VISUAL_SPEC` §12 신설** — 레퍼런스에서 유도한 수치 목표.
  네 라운드 동안 목표를 내가 지어내고 있었다는 것을 발견하고 만든 것이다
- **캡처 post ON/OFF 경계 규명** — 2026-08-02 커밋 `583fe08` 이전 하네스 캡처는 전부 post OFF.
  `Captures/baseline.txt` 는 이 기기에 **존재하지 않는다** (승인된 베이스라인이 없다)

## 최종 수치 (v5, post ON, 뷰 A·C·D 평균)

| 지표 | 목표 (레퍼런스 유도) | v5 | |
|---|---|---|---|
| g/r | 0.78 ~ 0.90 | **0.840** | 달성 |
| b/r | 0.45 ~ 0.62 | **0.513** | 달성 |
| `>0.50` | ≤ 0.5% | 0.15% | 달성 |
| mean | .055 ~ .075 | .0328 | 미달 → v6 에서 exp 0.70 채택 |
| p50 | .040 ~ .062 | .0186 | 미달 → 동상 |
| `<0.02` | ≤ 32% | 53.0% | 미달 → 동상 |
| p99 | .25 ~ .36 | .1454 | **미달 — 값 레버로 안 풀린다** (하이라이트 화소 수 문제) |
| 레버 대비 | ≥ 3× | A 6.01× / D 5.32× | 달성 |
| 대역비 | ≤ 5× | 1.91× | 달성 |

## 금지 21항 중 이 작업이 특히 걸리기 쉬운 것

- **#9 깨끗하고 대칭적인 쇼룸** — 새로 만든 셸은 마모가 없어 깨끗해지기 쉽다. 비대칭·얼룩을 남긴다
- **#11 플레이 불가능한 분위기 공간** — 어둡게 만들다 레버를 못 찾으면 분위기가 아니라 결함이다
- **#1 현대적 PBR** — smoothness 를 올리지 않는다
- **#21 전 화면 금속 glint** — 림 라이트를 재질마다 다르게 둔다

## 로그

- 2026-08-04 00:36 — 레퍼런스 이미지 저장, 기준선 캡처 4장
- 2026-08-04 00:5x — 블렌더 조립기 작성, 22,528 tris / 9 모듈 / UV 완비, FBX 내보냄
- 2026-08-04 01:0x — 채택 스크립트 작성, 컴파일 확인
