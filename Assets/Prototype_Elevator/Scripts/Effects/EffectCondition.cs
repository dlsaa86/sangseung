namespace Ascend.Prototype
{
    /// <summary>Condition that must be true before an effect can be applied.</summary>
    public enum EffectCondition
    {
        None,
        Overloaded,
        NotOverloaded,
        PerfectStop,
        ContainsGrade
    }
}
