namespace Ascend.Prototype
{
    /// <summary>Describes when an effect participates in a generation lifecycle.</summary>
    public enum EffectTrigger
    {
        OnHarvest,
        OnCombination,
        OnGeneration,
        OnFinal,
        OnOverload
    }
}
